# 项目执行规则

## 本项目特殊要求

- 项目范围仅限跟单管理系统（管理员网页端、服务器后端和微信小程序），不修改其他项目。
- 项目文档统一放入 `docs/`，按 `requirements/`、`project/`、`reference/` 等用途分类；临时草稿仅在确有需要时放入 `docs/drafts/`。
- 正式实现代码分别放入 `server/`、`admin-web/` 和 `miniprogram/`，不得把运行代码混入 `docs/`。
- 新增业务字段、状态流程或权限规则时，应在实现前明确其定义与验收标准。

## 一期范围护栏

- 正式业务范围和验收标准以 `docs/requirements/一期需求文档.md` V1.54 为准。
- 所有页面、字段、接口、导出和文档统一使用“合同出货时间”；一期包含已确认的质检单返修、按发货单退回和管理员手动获取飞书新订单流程，具体边界、数量口径、附件权限、状态口径、管理员跨端身份绑定与持久登录、候选、草稿、正式订单、部分发货导入与初始已发数量、合同编号稳定顺序、精简后的工厂资料规则、聚水潭六类启用产品同步范围、两类小程序通知中心、工厂登录时微信原生订阅授权触发规则及合同出货提醒飞书外发规则以 V1.54 正式需求为准。
- MySQL 8.0 是唯一正式业务数据库，飞书多维表格只作为订单上游来源，不自动回写。
- 正式实现使用新建的独立代码项目；旧 CloudBase 实现和 V1.5 原型仅作历史参考。

## 目标技术边界

- 正式后端部署在公司阿里云 ECS，正式数据库使用 MySQL 8.0；管理员网页端和微信小程序必须调用同一套 HTTPS API，客户端不得直接连接数据库。
- 项目按同一 Git 仓库的 `server/`、`admin-web/`、`miniprogram/`、`docs/` 分目录组织，三端业务边界不得混写。
- 开发、测试和生产数据必须隔离。本地 MySQL 可用于开发；正式环境应使用独立数据库和最小权限服务账号，并落实自动备份及恢复验证。
- 密钥、数据库密码和第三方凭证只能保存在服务器环境变量或受控密钥配置中，不得写入网页端、小程序端、Git、文档或日志。
- CloudBase 不再作为正式后端，也不得同时维护第二套正式业务数据库。

## 正式资料与执行顺序

- 开始阶段或工单前，必须先读取 `docs/project/一期里程碑与决策记录.md`，确认当前阶段和下一步，并读取当前阶段对应的已批准开发计划和工单。
- 需求文档约束业务范围和验收，批准的原型约束页面交互，批准的技术设计约束实现方式，批准的开发计划约束切片顺序和依赖，当前工单约束本次修改范围。
- 发现资料冲突时，不自行选择；先按上述职责确定应修改的正式资料，并在需要时取得用户确认。
- 对话记录、Agent 私有记忆和未确认草稿不能覆盖正式资料。
- 需求、原型、技术设计、开发计划和正式实施必须依次通过对应确认门，不得跳阶段编码。

### 原型页面开工确认门

- 制作或修改原型页面前，先列出本次展示内容、字段、操作和页面跳转；取得用户明确确认后才能修改页面代码。
- 用户限定“只需要”某项内容时即为范围边界；只处理本轮确认内容，不增加未确认的按钮、状态、统计、交互或其他可见内容。必要新增项先提出并等待确认。
- 一个页面确认完成后，进入下一个页面仍须重新执行本确认门，不能把前一页的确认自动扩展到后一页。

### 原型视觉约定

- 修改或实现小程序页面前，必须完整读取并执行 `docs/project/小程序原型复刻规范.md`；除该规范允许的平台适配、复杂图标简化和已确认不合理项外，正式页面必须严格复刻已批准原型。
- 本项目后续页面设计、原型制作和视觉调整默认不读取或调用 `kocotree-design` Skill；只有用户在当前任务中明确要求时才使用。
- 实施已有原型的正式前端页面前，必须打开对应原型并读取相关脚本、公共组件和样式；逐项对齐外壳、信息层级、字段、控件、文案、弹窗、空状态、排序分页和响应式布局。
- 优先复用已确认的 logo、图标、设计变量、尺寸、组件和交互；移植到正式公共组件或样式，运行代码不得依赖 `prototype/`，也不得提前开放后续工单功能。
- 完成前必须用实际浏览器与原型并排检查。构建和自动化不能代替视觉检查；有意差异写入当前工单，涉及业务规则或可见内容时先确认。
- 原型表单不显示红色必填星号；字段是否必填仍通过表单约束和提交校验实现。
- 表单字段名称与对应输入框保持紧凑横向间距，避免标签和控件之间出现过大的空白。

### 管理员网页端表格视觉规范

- 修改管理员网页端列表、表格、标题行、排序或分页前，必须完整读取并严格执行 `docs/project/管理员网页端表格视觉规范.md`，不得凭经验重新估算或在单页添加近似样式。
- 所有序号列必须以浏览器计算后样式达到 `52px` 宽、左右各 `8px` 内边距、水平居中、单行且不可排序；只声明 `width` 但实际发生换行或宽度偏差视为未完成。
- 规范与原型或工单冲突时，停止可见改动并请用户确认；确认后同步更新规范、实现和必要记录。

## 任务启动与交接

- 新任务开始时检查 `git status`、当前工单、前置依赖和测试基线。
- 只处理当前阶段或工单明确包含的内容，不顺带开发二期或无关功能。
- 任务结束时记录修改文件、验证结果、遗留问题和下一步范围；只有更换 Agent、暂停未完成任务或存在未提交修改时才写完整交接记录。
- 任何 Agent 接手前必须读取正式资料和交接记录，不得只依赖上一个 Agent 的总结。

## Agent 协作

- 同一时间只有一个 Agent 修改主工作区。
- 禁止两个 Agent 同时在同一个工作目录写文件。并行任务必须真正独立，并使用不同 Git worktree 或独立分支。
- 接手时保护已有修改，不回滚、覆盖或删除来源不明的内容；发现影响当前任务的冲突时先说明。
- `AGENTS.md` 是项目规则唯一正文；`CLAUDE.md` 只保留强制读取入口，避免重复维护和内容漂移。

## 实施、测试与安全

- 核心业务按批准的工单实施，服务器 API、MySQL 事务、权限、状态、数量和 Excel 导出优先采用 TDD。
- 原型页面修改默认不运行 Playwright 或其他浏览器自动化验收；只有用户在当前任务中明确要求验证、验收或测试时才运行。
- 未运行约定测试、构建或真实环境验证时，不得声称任务已经完成。
- 失败测试必须说明原因和影响，不得用删除测试或降低断言的方式掩盖问题。
- 正式数据、生产环境、付费资源、应用发布、数据覆盖或清理等高影响操作必须获得用户明确确认。
- 密钥、访问令牌、设备码、授权链接和其他凭证不得写入前端、Git、文档、日志或交接记录。
