# Claude Code 项目执行规则

本文件供 Claude Code 在跟单看板项目中执行任务时读取。其项目级约束与根目录 `AGENTS.md` 保持一致；Claude Code 自身的全局配置不能覆盖本项目已经确认的业务范围、正式资料和验收门禁。

## 本项目特殊要求

- 项目范围仅限跟单看板系统（管理员网页端、服务器后端和微信小程序），不修改其他项目。
- 项目文档统一放入 `docs/`，按 `requirements/`、`project/`、`reference/` 等用途分类；临时草稿仅在确有需要时放入 `docs/drafts/`。
- 正式实现代码分别放入 `server/`、`admin-web/` 和 `miniprogram/`，不得把运行代码混入 `docs/`。
- 新增业务字段、状态流程或权限规则时，应在实现前明确其定义与验收标准。

## 一期范围护栏

- 正式业务范围和验收标准以 `docs/requirements/一期需求文档.xml` V1.33 为准。
- 所有页面、字段、接口、导出和文档统一使用“合同出货时间”；一期包含已确认的质检单返修与按发货单退回流程，具体边界和数量口径以 V1.33 正式需求为准。
- MySQL 8.0 是唯一正式业务数据库，飞书多维表格只作为订单上游来源，不自动回写。
- 正式实现使用新建的独立代码项目；旧 CloudBase 实现和 V1.5 原型仅作历史参考。

## 目标技术边界

- 正式后端部署在公司阿里云 ECS，正式数据库使用 MySQL 8.0；管理员网页端和微信小程序必须调用同一套 HTTPS API，客户端不得直接连接数据库。
- 新代码项目按同一 Git 仓库、分目录组织，目标目录为 `server/`、`admin-web/`、`miniprogram/`、`docs/`；最终名称可在技术设计中确认，但三端业务边界不得混写。
- 开发、测试和生产数据必须隔离。本地 MySQL 可用于开发；正式环境应使用独立数据库和最小权限服务账号，并落实自动备份及恢复验证。
- 密钥、数据库密码和第三方凭证只能保存在服务器环境变量或受控密钥配置中，不得写入网页端、小程序端、Git、文档或日志。
- CloudBase 不再作为正式后端，也不得同时维护第二套正式业务数据库。

## 正式资料与执行顺序

- 开始阶段或工单前，必须先读取 `docs/project/一期里程碑与决策记录.md`，确认当前阶段和下一步。
- 需求文档约束业务范围和验收，批准的原型约束页面交互，批准的技术设计约束实现方式，当前工单约束本次修改范围。
- 发现资料冲突时，不自行选择；先按上述职责确定应修改的正式资料，并在需要时取得用户确认。
- 对话记录、Agent 私有记忆和未确认草稿不能覆盖正式资料。
- 需求、原型、技术设计、开发计划和正式实施必须依次通过对应确认门，不得跳阶段编码。

### 原型页面开工确认门

- 开始制作或修改一个原型页面前，必须先向用户列出本次拟包含的展示内容、字段、操作和页面跳转；取得用户明确确认后，才能修改页面代码。
- 用户明确限定“只需要”某项内容时，该表述就是本次工作的范围边界；不得根据常见产品做法、已有需求或 Agent 判断自行补充按钮、操作、状态、统计、交互或其他可见内容。
- 用户没有提到或没有确认的页面内容一律不做。认为有必要增加时，只能先提出建议并说明影响，等待用户确认，不能先实现再请用户删改。
- 用户对当前页面逐项修改时，只处理本轮明确提出的项目；为保持页面可运行而必须进行的技术性调整可以同步完成，但不得借此增加新的可见功能或业务行为。
- 一个页面确认完成后，进入下一个页面仍须重新执行本确认门，不能把前一页的确认自动扩展到后一页。

### 原型视觉约定

- 本项目后续页面设计、原型制作和视觉调整默认不读取或调用 `kocotree-design` Skill；只有用户在当前任务中明确要求时才使用。
- 原型表单不显示红色必填星号；字段是否必填仍通过表单约束和提交校验实现。
- 表单字段名称与对应输入框保持紧凑横向间距，避免标签和控件之间出现过大的空白。

## 任务启动与交接

- 新任务开始时检查 `git status`、当前工单、前置依赖和测试基线。
- 只处理当前阶段或工单明确包含的内容，不顺带开发二期或无关功能。
- 任务结束时记录完成内容、修改文件、测试结果、未解决问题、用户确认事项和下一步允许范围。
- 只有实际更换 Agent、暂停未完成任务或存在未提交修改时，才写完整交接记录；实施阶段优先写入当前工单。
- 任何 Agent 接手前必须读取正式资料和交接记录，不得只依赖上一个 Agent 的总结。

## Codex 与 Claude Code 协作

- Codex 和 Claude Code 均可执行任务，但不组成固定流水线；默认由一个 Agent 连续完成当前任务，高风险模块可由另一 Agent 只读审查。
- 同一时间只有一个 Agent 修改主工作区。
- 禁止两个 Agent 同时在同一个工作目录写文件。并行任务必须真正独立，并使用不同 Git worktree 或独立分支。
- 接手时保护已有修改，不回滚、覆盖或删除来源不明的内容；发现影响当前任务的冲突时先说明。
- `AGENTS.md` 与 `CLAUDE.md` 的项目级规则应保持一致；修改公共规则时同时检查另一份文件。

## 实施、测试与安全

- 核心业务按批准的工单实施，服务器 API、MySQL 事务、权限、状态、数量和 Excel 导出优先采用 TDD。
- 原型页面修改默认不运行 Playwright 或其他浏览器自动化验收；只有用户在当前任务中明确要求验证、验收或测试时才运行。
- 未运行约定测试、构建或真实环境验证时，不得声称任务已经完成。
- 失败测试必须说明原因和影响，不得用删除测试或降低断言的方式掩盖问题。
- 正式数据、生产环境、付费资源、应用发布、数据覆盖或清理等高影响操作必须获得用户明确确认。
- 密钥、访问令牌、设备码、授权链接和其他凭证不得写入前端、Git、文档、日志或交接记录。
