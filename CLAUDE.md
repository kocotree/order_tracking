# Claude Code 项目入口

根目录 `AGENTS.md` 是本项目唯一的项目级执行规则正文。

- 开始任何任务前，必须完整读取并遵守 `AGENTS.md`。
- 同时读取 `AGENTS.md` 指定的当前阶段正式资料、工单和专项规范。
- 不得以 Claude Code 的全局配置、历史对话或私有记忆覆盖项目正式规则。
- 项目规则只在 `AGENTS.md` 维护；本文件不复制正文，避免两份规则不一致。
