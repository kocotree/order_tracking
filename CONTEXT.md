# 领域术语

- **工厂**：系统中用于派工、筛选和人员归属的生产合作方，以日常使用的简称唯一识别。
- **工厂代码**：非空时为全局唯一的 1–32 位大写英文字母，与供应商编号独立；允许留空人工补齐，空代码不能首次生成合同，历史合同快照不变。
- **工厂合同资料**：生成加工合同时使用的工厂法定身份、联系方式和收款资料，与日常使用的工厂名称分开保存。
- **合同资料完整**：工厂代码、单位全称、单位地址和法定代表人均已填写，可用于导出加工合同。
- **加工合同**：只对应一个主订单和一个工厂的产品加工合同。
- **合同编号**：由签订日期、固定标识“KK”和工厂代码组成的唯一编号，不包含订单编号。同日同厂第一份合同使用无后缀基础编号，后续首次生成的第二份、第三份依次使用“-1”“-2”等后缀；已生成编号永不重排或修改。
- **合同文件名**：由合同编号、订单编号和产品名称组合形成的加工合同 Excel 文件名称。
- **加工单价**：不属于系统保存的业务数据，仅作为加工合同 Excel 中的空白填写项，由管理员导出后填写。

## 订单导入与发布

- **待处理候选订单**：管理员手动从飞书获取并保存在系统中的来源快照，尚未创建系统订单，不对工厂可见。
- **草稿订单**：管理员确认导入候选后创建的系统订单，可以继续核对和派工，但尚未发布，对工厂不可见、不可执行。
- **正式订单（已发布订单）**：草稿完成派工并由管理员发布后形成的可执行订单，相关工厂可以查看并按分配任务发货。
- **已导入**：候选记录已经成功创建对应草稿订单的导入结果，不等同于订单已经发布。
- **初始已发数量**：飞书来源在订单导入时记录的、导入前已经完成的历史发货数量基线；它不属于本系统发货记录，导入后不得由普通草稿编辑清零或调小。
- **系统有效发货数量**：本系统普通发货原报数量加上已确认收货差额，扣除已生效的作废和按发货单退回数量；不包含初始已发数量。
- **统一已发数量**：初始已发数量与系统有效发货数量之和，是页面、未发数量、进度和加工合同资格使用的唯一“已发数量”口径。

## 返修退回

- **质检单返修单**：通过仓库已有质检 Excel 创建并发送给工厂处理的独立任务，不关联原订单或正常发货单，也不改变正常订单进度。
- **按发货单退回**：管理员从有效正常发货单详情中选择产品规格，填写退回数量和原因，扣减来源订单对应规格已发数量并增加原工厂任务未发数量的操作；工厂通过原正常订单任务补发。
- **可退数量**：正常发货单某产品规格的确认数量（未确认时为原报数量）减去该明细此前已生效的累计退回数量；本次退回数量不得超过该值。
- **发货单退回操作记录**：按发货单退回后保留在原发货单和原订单日志中的数量变更凭据；不是独立业务对象，不生成独立单号、列表或状态。
- **质检单返修发回记录**：工厂针对质检单返修任务提交的一次实物发回记录，可以分多次提交，不属于正常发货单。
- **返修发货时间**：工厂在小程序提交一次质检单返修发回记录的系统时间；返修详情表格中只取年月日并显示为“发货日期”。
- **仓库退回总数量**：管理员根据质检单确认由仓库发给对应工厂处理的数量合计。
- **退回时间**：管理员上传质检单并确认创建质检单返修单的系统时间，默认记录当天实际创建时间，不由管理员手工填写。
- **返修数量**：工厂完成返修并随质检单返修发回记录返回仓库的数量。
- **报废数量**：工厂判断无法返修但仍返回仓库的数量，不代表工厂可以自行销毁。
- **返回数量**：一条工厂返修发回产品明细中的返修数量与报废数量之和。
- **仓库退回数量**：一条质检明细由仓库发给工厂的数量；工厂发回记录按产品规格展示时，为同一返修单内相同产品编码、产品名称和颜色/规格的质检明细数量合计。
- **返回总数量**：全部有效质检单返修发回记录中返修数量与报废数量的累计合计，即工厂累计发回的数量；工厂提交后立即生效，不经过管理员确认收回。

## 正常发货收货核对

- **原报数量**：工厂正式提交时按箱及订单派工保存的不可变数量；用于收货差异对照和发货清单下载。
- **收货核对草稿**：管理员网页按已有箱内明细保存的非负整数数量，只暂存，不影响业务数量或通知。
- **确认数量**：管理员整单确认收货时固化的箱内数量；确认后替代页面原报展示，按订单与SKU自动汇总。
- **收货差额**：确认数量减原报数量，正数为多收、负数为少收；确认时只追加一次有效数量变化。
- **已收货**：整单核对结果已正式确认，不等同于订单已完成，不替代发货单作废状态；确认时间不等于实际到货日期。


## 合同出货时间（V1.60，Issue #61）

- **明细合同出货时间**：归属订单＋SKU＋工厂的业务日期；不同明细允许不同日期。飞书原列“合同出货时间”减4个自然日得到初始日期，人工日期直接使用，不能重复减天数。
- **订单合同出货时间汇总**：当前可见明细日期去重并从早到晚排列的只读结果，不是独立可编辑的整单日期；工厂仅汇总本厂日期。
- **日期可编辑阶段**：管理员网页待导入和草稿阶段可改；已发布阶段不可改。日期缺失必须在导入／发布前补齐。
- **逾期明细**：未交完且当前业务日期晚于其合同出货时间的明细。已发布且未确认完成订单存在任一逾期明细即显示已逾期，已完成状态优先。
- 本轮为规则记录，业务代码尚未实施；补充确认规则、剩余方案边界与唯一执行工单见[Issue #61](https://github.com/kocotree/order_tracking/issues/61)和一期需求第3.4.1节。

- **日期人工覆盖**：待导入日期修改后自动保存；再次获取保留已保存人工日期，失败需明确反馈。相同订单＋SKU＋工厂的多来源日期冲突先留空，由管理员填写单一日期。
- **日期筛选与排序**：任一可见明细日期命中范围即保留订单；升序按最早日期，降序按最晚日期，缺失日期最后。
- **工厂任务逾期**：仅根据本厂未交完明细判断，与管理员所见整单逾期可不同；已完成状态优先。
- **同日期提醒分组**：同订单、同日期、同接收人的未交完相关明细合并提醒；工厂仅含本厂明细，不同日期分别提醒，原提醒节点不变。
