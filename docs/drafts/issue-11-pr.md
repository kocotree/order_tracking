## 关联

- Issue：#11（关联，不自动关闭；真机与共享测试验收待完成）
- 正式工单或需求：`docs/project/work-orders/S07-正常装箱发货闭环.md` Issue #11 节、`docs/requirements/一期需求文档.md` 第 4.2 节。

## 修改内容

工厂录入相同装法的连续箱时，无需逐箱重复选择产品和规格。当前箱明细卡片底部新增“应用到连续箱”，填写后续连续箱数后将完整明细复制到连续空箱；目标有内容或范围无效时整次拒绝。每箱可独立修改，草稿保存和恢复保留装箱组来源。产品、规格和订单下拉统一使用 SVG。

逐箱与批量均允许多发，数量归属沿用明细所选订单，复制不自动分配到其他订单；提交前刷新可发目录并确认多发明细。该口径已获用户确认并同步正式文档，替代 Issue 原“超过剩余可发数量被阻止”条款；Issue 正文尚待同步。

## 验证

- [x] 单元或集成测试：Node.js 24 下小程序 15 个测试文件、56 项通过。
- [x] 静态检查、构建或迁移检查：ESLint、TypeScript、项目结构构建、git diff --check、SVG 及资源引用检查通过。
- [ ] 浏览器或开发者工具检查（如适用）
- [ ] 真实外部或共享测试验证（如适用）

未运行或失败的验证：微信开发者工具编译、视觉及 iOS/Android 真机检查由用户自行进行，尚无完整验收记录；真实 API、共享测试未验证。项目结构构建不等于微信编译，远程完整 CI 待 PR 检查。

## 风险与回滚

复用现有草稿 groupKey 接口，无后端、数据库迁移或权限规则变更。多发确认基于提交前读取的目录快照，不新增数量预留。回滚本 PR 对应小程序改动即可恢复原入口；已有各实体箱数据仍沿用原接口读写。

## 发布边界

- [x] 本 PR 不代表已部署、已迁移、已发布小程序或已发送真实通知
- [ ] 推送标签、部署、迁移和真实外发已另行获得授权（如适用）
