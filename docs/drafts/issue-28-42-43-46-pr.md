通知单页入口进入订单、任务或返修详情后，原来直接返回会因缺少上一页而失效；本次接入现有角色核验及列表兜底返回，并按用户确认同步改善三个小程序页面的布局。

关联 #28、#42、#43、#46。用户明确授权合并交付。

## 变更

- #28：补齐管理员订单、工厂任务及两类返修详情；有上一页时返回原页，单页或返回失败时进入当前角色对应主入口。
- #42：订单卡片收紧间距和字号，商品名称在前、订单编号在后同一行，长文本省略。
- #43：全页文字黑色加粗；每个产品默认折叠，点击展开全部 SKU，收起保留输入；放大指定六个标签，删除“选择本次发回”。折叠是用户后续明确确认，替代 Issue 初稿的不折叠方案。
- #46：通知卡片填满正常边距内宽度，类型/时间同一行，标题一行、摘要最多两行。

## 验证

- Node.js 24：小程序 16 个测试文件、71 项测试通过。
- pnpm lint、pnpm build（含 typecheck、项目结构检查）、git diff --check 通过。
- 使用实际 WXML/WXSS 与样例数据做浏览器窄屏近似布局检查；不等同微信原生验收。
- 用户已确认 #28 修复；#42/#43/#46 微信开发者工具编译、上传及真实环境验收仍由用户执行。

只修改小程序和交付记录，无服务器或数据库改动。远程 CI 尚未运行；创建 PR 后需完整 CI 与审查。未部署或上传小程序。
