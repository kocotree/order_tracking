# Issue #26 本地交付与后续步骤

## 本地实施完成时状态

本次已建立 Issue、同步正式文档、建立分支并完成代码与本地测试。所有修改未暂存、未提交；没有 push、PR、新远程 CI、部署或真实消息发送。

- 工作区：跟单管理系统主工作区，分支 `codex/issue-26-notification-recipients`。
- 基线：main `60dbcbef26d5abadb2a1e894c81fb8d80234df62`；完整 CI 33952983090 成功。
- 工单：https://github.com/kocotree/order_tracking/issues/26 ，关联 #8。
- 正式依据：一期需求、技术设计及 S11 的 Issue #26 追加节。

## 修改与口径

- `server/app/modules/notifications_audit/service.py`：固定名单加入核桃；发货及撤回通过发货明细的订单关联查询跟单人，精确姓名或花名唯一匹配、启用过滤、按用户去重；缺失/歧义/停用记录诊断。
- `server/tests/integration/test_notifications_audit.py`：固定五人加订单跟单人，真实业务服务创建多订单发货/撤回、跨订单去重、同厂无关订单排除及失效身份；返修仅固定五人。
- 正式需求、技术设计、S11、里程碑已同步；无需数据库迁移、API、卡片、网页端或小程序变更。
- 返修不关联订单，用户确认只通知固定五人，订单关联另议；D3、站内通知、工厂通知及历史任务不变。
- 飞书 CLI 当前用户身份有效，核桃唯一完整名称为杨嘉彬&核桃且已激活。未把 CLI 身份 ID 写入项目；真实业务应用绑定和送达仍待部署后验证。

## 验证

- TDD 首次失败确认原实现漏掉核桃和订单跟单人，修复后通过。
- MySQL 8 通知专项：31 passed（43.79 秒），覆盖 notifications_audit、feishu_notifications、wechat_notifications 和 notifications_api。
- Ruff 全 app/tests 通过；mypy 全 app 67 文件通过；git diff --check 通过。
- 未跑本次全量后端、远程 CI、Docker、真机或真实飞书发送。测试发送器结果不代表真实送达。

## 后续范围

本轮停在用户要求的第 4 步。后续先审阅差异并按用户指令本地提交，再按各 Issue 分支分别推送和建 PR、通过完整 CI、合并后检查 main CI，最后统一部署。不要把本轮未提交文件带入其他 Issue 分支；并行使用其他工作目录。

#16 当前为工厂字母代码规则与存量迁移确认单，可并行做规则确认和映射梳理；字符集、长度、映射、生效范围、历史保护及回滚确认后才能拆执行单。其代码/数据实施不得在本工作目录同步修改；共享的正式需求和决策文档在集成时逐节协调，历史合同编号不改。

## 后续交付授权

用户随后授权提交、推送并创建 PR，由用户自行合并。以下发布操作继续不在本轮范围：部署、迁移、小程序发布和真实消息发送。远程 CI 由推送/PR 自动触发，实际结果以 GitHub Actions 为准。
