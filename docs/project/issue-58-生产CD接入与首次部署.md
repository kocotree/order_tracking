# Issue #58 生产 CD 接入与首次部署

## 已确认范围

用户确认沿用公司通用 GitHub Actions + Docker 方案，授权推进跟单管理系统正式部署。
本次不改业务功能，不重复迁移资料，不发布微信小程序或发送真实通知。
用户明确接受此次测试与正式共用现有 MySQL 账号；数据库和 OSS 桶独立。
这是本次上线的已接受例外，不代表已经具备数据库账号级环境隔离。

## 首次数据准备证据

正式数据库结构为 20260907_0030。产品 459、规格 5053、工厂 80、联系人 65；
测试工厂 S12T01 已排除。其余业务表为空。四张资料表逐字段与恢复副本比对通过。
产品图片 442 个对象复制后重新读取并通过 SHA256 校验。迁移后 MySQL 备份恢复通过。
临时恢复容器和本次下载的 MySQL 镜像已清理，备份保留在服务器受控目录。

## CD 实现与发布顺序

1. 独立 Issue/分支修改，通过 PR、完整 main CI。
2. 发布已批准的 v* 标签，完整 CI 后发布两类 GHCR 镜像。
3. 手动运行 Deploy production，选择同一标签；完整 CI 和镜像发布检查通过后部署。
4. 校验 SSH 主机身份，部署凭据通过 GitHub Secrets；registry 使用短期 GITHUB_TOKEN。
5. 服务器校验两个镜像 revision，备份 MySQL/OSS，再迁移、启动、健康检查和记录版本。
6. 外部 HTTPS、飞书登录回调和真实用户授权单独验收；小程序审核发布单独执行。

生产 Compose 仅拉取 GHCR 镜像。旧 Git/build 发布脚本继续服务共享测试。
回滚使用上一版本镜像和配置，不自动降级数据库。失败时不记录发布成功，
但数据库 DDL 或已替换的容器可能需要人工处理，不宣称全流程原子回滚。

## 当前验证与待办

本地五项部署失败边界测试通过；Shell 语法和生产 Compose 解析通过。
远程 CI、CD Secrets 配置、正式服务启动、正式 HTTPS 与用户授权尚未完成。
每日自动备份调度、告警和持续恢复演练不因本次发布前备份自动视为完成。
