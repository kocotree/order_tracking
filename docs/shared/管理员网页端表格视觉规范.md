# 管理员网页端表格视觉规范

> 状态：2026-08-22 用户确认
> 适用范围：`admin-web/` 中已有和后续新增的后台列表页、数据表格、表格标题行、状态标签、排序与分页。
> 原型基线：`prototype/admin-web-lowfi/` 中对应页面、公共组件和最终加载的样式。
> 正式实现基线：`admin-web/src/`。

## 1. 新会话或新 Agent 的必读顺序

修改管理员网页端列表或表格前，必须依次完整读取：

1. `docs/一期/project/一期里程碑与决策记录.md`，确认当前阶段；
2. 当前阶段的已批准工单；
3. 本规范；
4. 对应原型页面脚本、公共组件及全部相关样式；
5. 正式页面组件和公共样式。

不得只看截图、只看某一条 CSS、只凭上一个会话总结或凭其他页面经验直接修改。

## 2. 资料职责和冲突处理

1. 需求文档和当前工单决定正式字段、业务文案、数据、权限、状态和操作范围。
2. 已确认原型决定页面外壳、信息层级、控件顺序、布局、表格视觉和交互方式。
3. 本规范记录已经确认的可重复尺寸和页面级精确参数。
4. 正式工程现有样式只是待核对实现，不得反过来覆盖原型或本规范。
5. “与原型对齐”不等于复制原型演示数据。不得复制“低保真原型”标识、原型专用说明、假订单数量或假账号；正式页面继续显示真实接口数据和正式业务文案。
6. 需求、工单、原型和本规范发生冲突时，不得静默选择，也不得先改再说明；停止可见改动，列出冲突并取得用户确认，确认后同步更新本规范和对应工单。

## 3. 实施方式

1. 优先复用公共表格类、排序按钮、标签、分页和设计变量，不得为单页另写一套近似样式。
2. 不得用其他页面的行高、字号或列宽推算当前页。不同业务表可以有不同密度，必须使用下方页面级参数或对应原型的最终计算样式。
3. 同一元素命中多份 CSS 时，以浏览器最终计算样式为准。后加载规则、选择器优先级和表格自适应都必须纳入判断。
4. 表格列宽要求指浏览器中的实际渲染宽度。只写 `width`、但最终发生换行或宽度偏差，仍视为未完成。
5. 横向空间不足时保留页面既定 `min-width`，由表格外层横向滚动，不得压缩到表头换行、操作重叠或列宽失真。
6. 只处理用户当前确认的页面和项目，不借视觉对齐增加按钮、页签、字段、状态或业务行为。

## 4. 所有后台数据表的公共规则

| 项目 | 固定要求 |
|---|---|
| 页面内容卡片间距 | `14px` |
| 表格卡片标题行 | 高度 `54px`；内边距 `0 16px`；内容垂直居中；底部 `1px` 分隔线 |
| 标题文字 | `18px`；字重 `800`；单行显示 |
| 表头行 | 高度 `40px`；字号 `13px`；字重 `800`；文字色 `#37404b`；背景色 `#f1f3f6` |
| 单元格边框 | `1px solid #dde1e7`；使用 `border-collapse: collapse` |
| 普通单元格 | 默认左右内边距各 `14px`；垂直居中；超长文本单行省略，不得随意换行撑高 |
| 序号列 | 实际宽度 `52px`；左右内边距各 `8px`；水平居中；单行；字重 `600`；不得排序 |
| 排序列 | 仅已确认业务列显示公共双箭头；再次点击在升序和降序间切换 |
| 非排序列 | 序号、图片、选择框、操作等辅助列默认不排序，除非用户另行确认 |
| 操作区 | 操作项保持单行，已确认双操作页面的间距为 `12px`，不得粘连 |

## 5. 订单列表的精确基线

对应正式路由：`/orders`。对应原型路由：`#/orders`。

### 5.1 页面与数据行

- 顶部标题显示“订单列表”，不得显示模块名“订单与发货”。
- 表格类为 `.order-list-table`，`min-width: 1132px`，`table-layout: fixed`。
- 表头高度 `40px`；数据行高度 `40px`，不得再设为 `56px`。
- 普通单元格为 `13px / 700`，上下内边距为 `0`，左右各 `14px`。
- 序号单元格为 `13px / 600`，左右各 `8px`，居中。

### 5.2 十一列宽度

按“序号、订单编号、产品名称、分类、跟单人员、工厂、合同出货时间、发货进度、已发/订单数、状态、操作”的顺序：

`52 / 82 / 164 / 68 / 84 / 110 / 142 / 160 / 110 / 96 / 104px`

在表格可用空间大于最小宽度时，固定布局可以按同一比例扩展；不得单独放大订单编号、分类或跟单人员列，导致比例偏离原型。

### 5.3 标签与操作

- 分类和跟单人员使用原型中的圆角标签；跟单人员颜色按姓名沿用原型映射。
- 状态使用 `.status-badge`；高度 `22px`、最小宽度 `44px`、内边距 `0 10px`、字号 `13px`、字重 `700`，订单列表状态标签不显示前置圆点。
- 状态色：草稿为紫色 `#65547b / #f0ebf7`；已逾期使用危险色；未完成使用信息色；已完成使用成功色。
- “详情、删除”保持同一行，间距 `12px`。是否显示删除由正式业务规则决定，不照搬原型演示行。
- 页脚继续显示正式接口总数和真实分页；不得复制原型的“原型数据仅用于验证……”提示。

## 6. 待导入订单列表的精确基线

对应正式路由：`/orders/import`。对应原型路由：`#/pending-imports`。

- 表格类为 `.import-candidate-table.pending-import-table`，`min-width: 1040px`，固定布局。
- 表头和数据行均为 `40px`；表格字号 `13px`；普通单元格字重 `700`。
- 九列按“选择、序号、订单编号、产品名称、分类、跟单人员、工厂、校验状态、操作”的顺序：

`44 / 52 / 100 / 228 / 82 / 112 / 142 / 144 / 100px`

- 选择列和序号列左右内边距各 `8px`、居中；两列与操作列不排序。
- “获取飞书新订单”和“批量导入为草稿”之间保持 `12px`，不得粘连。
- 校验状态标签高度 `22px`、字号 `13px`、字重 `700`，不显示前置圆点。
- “详情、删除”保持单行且有明确间距；不得依赖空格字符撑开。
- 每页显示 `10` 条；页脚显示真实总数和正式分页。

## 7. 订单详情的精确基线

对应正式路由：`/orders/:orderId`。对应原型路由：`#/orders/:orderNo`。

- 顶部标题为“订单详情 · 订单编号”；主体卡片间距 `14px`。
- 概览卡片顶部操作行最小高度 `54px`、内边距 `9px 18px`；返回按钮、状态标签和操作按钮的顺序、尺寸、圆角均以原型为准。
- 概览信息使用三列两行布局，只显示“分类、跟单人员、合同出货时间、订单数量、已发数量、未发数量”；每格最小高度 `58px`，字段名单元格最小宽度 `118px`。
- 订单明细表为十列：序号、图片、产品编码、产品名称、颜色/规格、工厂、下单数量、已发数量、未发数量、发货进度。最小宽度 `1080px`，表头高度 `40px`，数据行高度 `48px`，字号 `14px`。
- 十列宽度依次为：`56 / 72 / 110 / 210 / 135 / 86 / 96 / 96 / 96 / 160px`。序号、图片不排序，其余八列使用公共双箭头排序。
- 关联发货单保留原型表头和表内空状态；S07 前不得制作可进入的发货单详情。
- 订单详情页不显示独立的“工厂派工与进度”卡片；工厂、数量和进度由订单明细表承载。详情底部新增只读“操作日志”，仅显示时间、操作人、操作内容，不提供编辑、删除、筛选、分页或独立日志页面。
- 当前正式业务与原型只保留阶段门禁差异：草稿继续显示“编辑草稿、删除订单”；S06 前不得开放原型中的“导出加工合同”。

## 8. 其他已确认页面密度

未列出精确业务列宽的页面，仍必须先读取其对应原型，不得只按本表推断。

| 页面 | 表格 | 最小宽度 | 表格字号 | 表头高度 | 数据行高度 |
|---|---|---:|---:|---:|---:|
| 订单看板 | `.dashboard-order-table` | `1120px` | `13px` | `40px` | `40px` |
| 产品资料 | `.product-list-table` | `860px` | `14px` | `40px` | `40px` |
| 工厂资料 | `.factory-list-table` | `1270px` | `14px` | `40px` | `48px` |
| 人员管理 | `.people-table` | `880px` | `13px` | `40px` | `40px` |

人员管理中的工厂用户申请、管理员账号和用户列表共用同一套标题行、表头、序号列和排序规则。

人员管理中的“申请状态”和“启用状态”标签只显示文字和状态底色，不显示前置圆点；该规则同时适用于工厂用户申请、管理员账号和用户列表。

## 9. 完成前浏览器对照清单

后续实施任务除非用户当次明确取消检查，完成前必须在同一视口并排打开正式页与对应原型，并记录：

1. 顶部标题、卡片结构、控件顺序和文案；
2. 表格总宽、各列实际宽度、表头高度和数据行高度；
3. 表头、普通单元格、序号、标签和操作文字的计算后字号与字重；
4. 单元格上下左右内边距、对齐、单行省略和横向滚动；
5. 状态标签的高度、颜色、圆点显示与否；
6. 排序列、非排序列、分页和空状态；
7. 任何因正式业务数据、权限或文案保留的有意差异。

lint、类型检查、单元测试和构建只能证明代码质量，不能代替上述视觉对照。


## Issue #14 已确认删列适配（2026-09-06）

用户确认产品资料页保留图片并增加预览，订单详情、待导入订单详情和发货单详情删除产品图片列，原型文件不修改。本节覆盖第 7 节旧十列表述及对应原型图片列；其余表格规则继续执行。

- 订单明细改为九列：序号、产品编码、产品名称、颜色/规格、工厂、下单数量、已发数量、未发数量、发货进度。
- 待导入明细改为十列：上述订单明细字段加校验结果；发货明细改为六列：序号、关联订单、产品编码、产品名称、颜色/规格、发货数量。
- 删除图片列时同步调整列序号选择器和 colgroup，不把原图片列宽误用到下一业务列。保留既定最小宽度和横向滚动，产品名称列吸收可用余量，其他业务列沿用既有宽度设置。
- 本次四张表序号列均固定为实际 52px、左右各 8px、居中、不排序；序号数据字重 600。产品资料表产品名称列吸收余量，避免全列定宽造成序号列被比例拉伸。
- 产品资料预览为居中弹层，图片等比例显示；支持关闭按钮、点击遮罩和 Esc，Tab 保留在弹层内，关闭恢复触发位置并保留列表状态。无图或加载失败不可打开预览。

## 2026-09-06 Issue #17 全站分页确认

订单列表、待导入订单、发货单列表、返修退回、产品资料、工厂资料及人员管理三个页签统一每页 10 条。总页数不超过 7 时全部显示；超过 7 页时最多 7 个位置（含省略号，不含左右箭头）：开头 `1 2 3 4 5 … N`、中间 `1 … p-1 p p+1 … N`、结尾 `1 … N-4 N-3 N-2 N-1 N`。省略号不可点击，当前页高亮。订单看板仅展示 10 条，不显示页码，“查看全部订单”进入订单列表。

人员列表按权限、筛选、全局排序后在服务端分页；其他列表沿用完整数据或既有分页，不得截断首批记录。搜索、筛选和排序变化后回第一页；删除、审核、导入、归档等造成越界时回有效页；序号跨页连续。保留既有字段、操作及权限，不增加每页条数选择器。

用户明确本轮不读取或修改历史原型，分页外观沿用正式产品资料页；本节覆盖旧分页约定，其他表格视觉要求继续执行。

为同时保留各业务列基准宽度和序号固定 52px，订单列表、待导入订单、工厂资料、返修列表由产品名称、单位全称或工厂名称列吸收宽屏余量；最小表宽按既有各列宽度合计分别为 1172、1104、1287、1222px，窄屏沿用外层横向滚动。覆盖此前小于列宽合计的 min-width 声明，避免弹性列被压缩或序号被比例放大；不增删字段。

## 2026-09-07 Issue #47 已确认列宽与通知展示

用户确认看板订单编号120px、单行省略且悬停显示完整编号，保持详情跳转；产品名称吸收剩余宽度。采用固定布局，序号52px，其余分类68、跟单84、工厂110、合同出货时间142、进度160、已发/订单数110、状态96px；最小表宽1192px（产品名称250px），宽屏由产品名称吸收余量。

待导入订单详情沿用已确认的无图片十列：序号52、编码160、名称弹性且至少200、规格120、工厂68、下单/已发/未发数量各96、进度144、校验110px；最小宽度1142px。两张表宽度不足由外层横向滚动；序号左右内边距8px、单行居中不可排序。编码、产品名称提供完整文本title。

通知记录类别字号由11px增至14px；类别、标题、摘要和时间均使用黑色#000，已读保留较轻字重，读态圆点与筛选高亮继续沿用现有机制。仅网页通知记录页改字号和配色，管理员小程序及网页其他通知入口同步内容，不照搬尺寸。

本轮不运行Playwright或微信开发者工具，声明尺寸经代码和构建验证，不表示浏览器计算宽度或真机视觉验收。

## 2026-09-07 Web 原型反向同步

用户本轮授权按当前前端代码更新 Web 原型，并自行打开验证；不运行浏览器或小程序工具。本次原型对照基准为 `50747abd`，覆盖此前 #14/#17/#47 等“本轮不修改原型”的历史停止点，仅用于本次已实现页面同步，不改变正式业务需求。

原型 `index.html` 当前加载 `styles/frontend-baseline.css` 和 `styles/prototype-adapter.css`；其他旧样式不再参与渲染。当前前端列宽、字体、行高的源代码值已复制到独立原型样式，未取得浏览器计算后尺寸证据。逐页结果、范围例外及验证见 [Web 原型同步差异清单](../一期/project/Web原型同步差异清单.md)。手工新建/编辑草稿的订单日期冲突未同步，不声称所有 Web 路由已完全一致。
## 2026-09-07 Issue #45 关联发货单四列确认

用户确认订单详情关联发货单仅保留“发货单号、发货日期、发货数量、操作”，单号及“详情”进入对应发货详情。本节覆盖第7节保留旧原型表头的约定；本表不增加序号或排序。

- 正式页与原型统一四列，固定布局、最小宽度900px；单号吸收余量，日期160px、数量140px、操作100px。窄屏保留外层横向滚动。
- 加载中、错误、无记录提示单元格覆盖四列。既有整页首次加载行为保持。
- 只移除本表物流单号占位及状态展示，正常、撤回处理中、已作废记录仍按当前订单关联查询；单号和详情入口保持。
- 本轮用户授权本地修改、测试及提交后停止，明确不运行Playwright。未进行浏览器视觉测量，不将CSS声明宽度描述为实测验收通过。

## 2026-09-07 Issue #56 Web 横向布局调整

用户确认按真实内容重新分配列宽，截图红框不作为精确尺寸；所有 Web 页面主体横向铺满可用内容区域，保留既有内边距，不纵向拉高数据行。移除主体 1800px 上限。本节覆盖此前指定页面由产品名称/工厂吸收全部余量的规则。

各表序号固定52px。以下为最小表宽时的业务列宽基准，除固定辅助列外按比例分配剩余空间，窄屏外层横向滚动：

- 订单看板：最小1412px；序号之后依次120/420/90/90/100/142/150/150/98px。
- 订单列表：最小1516px；业务列沿用看板比例，另保留固定104px操作列。
- 待导入订单：最小1206px；选择44、序号52、订单编号90固定，其后名称340、分类120、跟单120、工厂140、校验180、操作120px；宽屏保持其他业务列此前比例，原订单编号缩窄的余量全部转给产品名称。
- 产品资料：最小1112px；序号52、图片90固定；货号/编码/名称/颜色规格依次150/200/300/320px。
- 返修退回：最小1262px；序号之后依次180/140/110/110/140/160/150/110/110px，工厂不再独占余量。
- 人员管理三个页签普通表格内容统一700字重，已有辅助列规则保留。
- 发货详情收货草稿保存提示复用待导入详情 validation-callout：浅黄色底、11px/13px内边距、5px圆角，外边距0；原提示文案、显示条件及确认收货业务不变。用户截图的红框仅为标注，不实现红框。

本轮用户明确不运行 Playwright，由用户自行浏览器检查；上述为代码声明及分配基准，非浏览器计算后宽度证据。原型和小程序本轮不修改。

用户本地查看后追加确认：收货成功不再显示中间「收货已确认」提示，保留顶部已收货状态、操作人和时间；两页产品名称基准280增至360px，待导入订单编号改固定90px。继续由用户自行浏览器检查。

用户继续反馈名称列加宽不明显：两页保持最小表宽不变，将分类/跟单人员/工厂基准110/110/120缩为90/90/100px，合计60px转给产品名称（360→420px）；宽屏按同一比例转移，其余列不变。

用户再次反馈两页没有可见变化：本地服务已确认提供更新样式，但未进行浏览器实测。取消看板/订单列表表头百分比减像素的混合calc表达式，除产品名称auto外使用上述明确px宽度；名称吸收余量，最小表宽下420px。移除看板数据单元格旧260px最大宽度约束。分类/跟单/工厂始终90/90/100px，其余列不再随宽屏同比扩展；仅本次两页覆盖上文比例规则。

### Issue #56 最终列宽修正（以本节覆盖前述尝试）

用户截图证实产品名称auto重新造成约810px的大块空白，取消该方案。两页产品名称使用纯百分比30%，不使用混合calc，也不独占余量。看板编号/名称/分类/跟单/工厂/日期/进度/数量分别8/30/6/6/7/11/11/11%，序号固定52px，状态吸收不足10%的余量；订单列表对应8/30/5/5/6/10/10/10%，序号52px、操作104px，状态吸收剩余空间。最小表宽仍为1412/1516px，窄屏外层滚动。以1800px表宽为例，名称为540px；实际效果由用户浏览器核对，未实测。


## 2026-09-08 Issue #61 日期列调整范围（待页面方案确认）

用户确认待导入明细增加“合同出货时间”，从产品名称列腾出空间，并调整颜色／规格和工厂列宽；顶部日期去重升序汇总。此前待导入详情十列及规格120px／工厂68px等尺寸不是本次最终目标。新列顺序、具体宽度、控件与长日期展示待逐页原型确认，本轮不写入未经确认的精确像素，也不修改页面。序号实际52px、左右各8px、居中不可排序和其他未受影响公共规范继续执行。

唯一执行工单见[Issue #61](https://github.com/kocotree/order_tracking/issues/61)，业务日期规则以[一期需求 V1.60](../一期/requirements/一期需求文档.md)为准。


## 2026-09-08 Issue #61 本地实施列宽与反馈

用户随后授权代码、测试及本地提交，取消Playwright检查。正式页沿用现有概览、表格和操作布局，以下是代码声明值，尚无浏览器计算尺寸或用户视觉验收证据；原型未同步。

- 待导入明细十一列：序号、产品编码、产品名称、颜色/规格、工厂、合同出货时间、下单数量、已发数量、未发数量、发货进度、校验结果。序号52、编码160、名称吸收余量、规格156、工厂120、日期174、三个数量各96、进度144、校验110px；最小表宽1404px，名称最小基准200px。比旧表从名称余量中给日期、规格和工厂分配空间。
- 正式订单详情同样在工厂后增加只读日期，编码维持110px、无校验列；最小表宽1294px，其他列基准同上。序号左右8px、不可排序；新增日期沿用公共排序控件。
- 待导入日期输入框高至少32px，编辑后自动保存，保存过程中禁用日期及导入；保存失败显示可读错误，版本冲突提示刷新。源日期为空不添加异常横幅；确认导入时提示SKU／工厂并聚焦空日期，弹窗确认期间禁止重复导入。
- 草稿把原顶部日期控件改为只读汇总，每个工厂派工行增加日期输入框，仍由“保存草稿”提交。已发布详情不增加编辑入口。
- 顶部、列表和看板显示去重升序日期集合，长内容允许换行，列表提供完整title。小程序保留卡片／明细结构，汇总显示可见日期集合，明细产品名称下补只读日期；不增加操作与页面跳转。

### 2026-09-08 本地查看追加：关联发货单等宽

用户确认订单详情“关联发货单”操作列保持100px，发货单号、发货日期、发货数量三列平分剩余宽度。固定表格布局下三列均使用auto，保留900px最小表宽及横向滚动。本项覆盖Issue #45单号吸收余量、日期160px及数量140px的旧设置；不改订单明细表。继续由用户查看本地页面，不运行Playwright；原型未同步。


## 2026-09-11 Issue #87 明细分批派工原型（布局已授权，视觉待评审）

详情订单明细增加独立选择列及工厂后的派工状态列；日期沿用明细日期规则。标题行展示派工进度和两按钮；问题不放表格，仅在派工弹窗。列表不加列/页签。序号保持实际52px、左右8px、居中单行不可排序；选择列独立于序号。其他列优先固定，名称吸收余量，空间不足横向滚动；全派工隐藏选择列。新增宽度以原型实测为评审证据，不把历史nth-child规则错用到插列后的位置。


### 2026-09-11 Issue #87 编辑权限与视觉范围最终修订

用户确认：订单详情的未派工明细仅合同出货时间可以直接修改；其他字段（包括工厂匹配、产品资料、下单数量、已发数量）全部只读。此前允许本页人工工厂匹配及其他字段编辑的说明由本条覆盖；来源资料修正通过飞书更新，系统工厂/账号问题仍由相应资料管理处理。人工日期更新保护保留。

正式前端仅“订单详情 → 订单明细”的列宽、样式按本次原型实施；其他页面及本页关联发货单等表格列宽不修改。订单明细表头字重800、所有内容含序号/日期/进度字重700，覆盖本表序号旧600约定；序号宽52px、左右8px、居中单行不可排序不变。当前只改原型及资料，正式代码尚未授权。
