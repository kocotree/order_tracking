# Issue #56 Web 本地交付与后续步骤

> **2026-09-08 状态核验：** Web 与小程序变更已由 [PR #57](https://github.com/kocotree/order_tracking/pull/57) 合并，Issue #56 仍开放；后续环境与视觉验收按 Issue 跟进。
>
> 下文保留当时实施、授权停止点和验证证据，均属历史记录。提交、推送、建 PR 的旧待办已被上述合并结果替代；其余真实验收不得仅凭合并推定通过。

## 范围与停止点

用户授权仅修改 Web、测试、本地提交后停止；另明确跳过 Playwright，由用户自行浏览器检查。基于 main 555863f（远程完整 CI 成功）建立 codex/issue-56-web-layout，工作区起始干净。

修改 styles.css、RepairsPage.vue、ShipmentDetailPage.vue 及表格视觉规范：主体取消1800px宽度上限，指定列表合理分配列宽，人员表格加粗，草稿保存提示复用现有浅黄色横条。保持业务字段、操作、排序、分页、权限和数量规则。

## 验证与边界

Web 65项测试通过；ESLint、类型检查及构建通过。未执行浏览器检查，无实际渲染尺寸或视觉验收证据。原型未修改，新的有意差异以表格规范 Issue #56 节和 GitHub #56 为依据。

本地前端预览使用项目默认5175端口；API默认代理本机8000端口。当前本机API未启动，前端可访问但业务数据浏览依赖后端服务，不能将前端HTTP成功视为端到端可用。

## 后续

用户先检查 Web。工厂小程序隐藏收货人时间行及差异提示改浅黄色黑色粗体尚未实施，仍属同一个 Issue；另获授权后在本分支单独提交，最终一起推送并创建一个 PR。本轮不推送、不开PR、不部署、不修改飞书状态。Issue整体未完成。

## 本地视觉反馈跟进

本地API随后已启动，开发库迁移至0030，启动前备份留在本机临时目录；演示模式使用本地数据与假身份。用户截图反馈后，取消确认收货后的中间成功提示；看板/订单列表产品名称基准增至360px，待导入编号固定90px，释放空间全部补给产品名称。Web 65项测试、Lint、类型检查、构建通过；继续不运行浏览器检查，仅本地提交。

后续再次按用户反馈调整两页列宽：产品名称基准420px，分类90、跟单90、工厂100px；从后三列合计转移60px，表格最小宽度及其余列保持不变。

两页仍无可见变化的反馈：源码及本地HTTP返回均包含最新样式，未发现后续覆盖；混合calc在表格中的实际布局仍未浏览器验证。改用业务列明确px宽度、产品名称auto的分配方式，移除看板旧max-width约束。用户继续自行验证。

最新截图显示名称auto占约810px，回退了合理分配目标；已纠正为两页名称纯30%及其余业务列各自百分比，固定序号/操作，状态平衡余量。最终参数以表格规范最后一节为准；不再采用此前名称auto方案。

## 工厂小程序本地交付

用户已确认Web最终效果，并授权同一分支修改小程序、测试及本地提交，仅提供push与PR指令。修改factory-shipment-detail.wxml和wxss：删除已收货操作人时间展示行，差异区域改为浅黄底#fff6df、黑色#000、700字重、26rpx字体、22/26rpx内边距和10rpx圆角；长文本换行、多条差异分行，保留标题、内容与原显示条件。

按当前已确认截图覆盖历史原型的相关收货提示视觉；不修改管理员小程序、后台数据或业务逻辑。小程序83项测试、Lint、类型检查和项目结构构建通过。微信开发者工具及真机视觉检查未执行，不能视为真机验收通过。

本地提交后停止，不push、不创建PR、不修改飞书状态。PR说明现见已合并的[PR #57](https://github.com/kocotree/order_tracking/pull/57)，使用Refs #56关联，待小程序视觉及后续环境验证后再关闭Issue。
