# Issue #79 质检上传修正

用户于2026-09-10确认文件行增加移除入口。未创建文件右侧显示×，移除后不参与确认；处理期间禁用，已创建项不可移除。不删除服务端预览、原文件或正式业务记录。

生产日志证实上传入口返回413，Nginx默认大小限制与解析器20 MiB限制不一致。仅质检上传路径允许21 MiB请求（含表单开销），前端限制单文件20 MiB并明确提示413。用户追加确认本次上传列表最多20个文件（含成功、失败及已创建项），点击和拖入共用累计限制；超出剩余容量时整批不加入并提示，移除未创建项后释放名额。仍逐文件串行上传。解析器仍限制5000行、10个工作表及解压后100 MiB。

验收：移除通过项后不创建；错误项可移除并重选；处理中不能移除；成功创建项不显示移除；超限文件不发请求；20 MiB边界可发送；413展示明确原因。

关联 https://github.com/kocotree/order_tracking/issues/79 。本轮不部署、不改生产数据，不使用Playwright；页面效果由用户验证。

本地验证：返修页面专项11项通过，TypeScript、相关文件ESLint、Web构建、Nginx容器配置检查及git diff --check通过。未执行浏览器视觉验收、远程CI或部署。

20个文件限制追加验证：专项12项通过，覆盖一次21个拒绝、累计添加、拖入边界和移除后重加；类型及相关ESLint通过。
