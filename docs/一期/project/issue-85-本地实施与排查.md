# Issue #85 小程序登录文案与失败重试

2026-09-10，基于 main `50b747e`（对应 main CI 成功），独立分支 `codex/issue-85-mini-login`。用户要求两角色登录按钮改为“手机号快捷登录”，并排查提交审核时已勾选协议却提示同意协议的现象。

## 已证实问题与修改

管理员和工厂共用 `miniprogram/pages/auth`。原手机号回调将协议状态和身份绑定凭证合并判断，任一不满足均提示同意协议；身份识别失败后凭证为空，但仍可打开手机号授权且没有身份重试路径。页面回调测试先在原代码失败，修正后通过。

共用按钮和两角色原型按钮文案已同步；协议未勾选才显示协议提示，缺少身份凭证时原按钮重新识别，识别完成后再次点击触发原生手机号授权。识别前清除旧凭证、请求期间拦截重复操作。既有权限、账号绑定、手机号授权和角色跳转不变，无后端与数据库变更。

## 验证

- 小程序全部 90 项测试通过（含新增 7 项页面行为测试）：已勾选但凭证缺失不误报、失败后重试并保留勾选、未同意阻断、请求中防重、管理员和工厂绑定后各自跳转、拒绝手机号授权后可重试。
- ESLint、TypeScript 和小程序项目构建检查通过。
- 线上只读检查：测试与正式 API 的未登录 `/api/v1/me` 均按预期返回 401。检查时近 3 小时服务器日志：共享测试微信识别 1 次 200，正式微信识别 3 次 200、手机号绑定 1 次 200；不能据此认定截图请求已成功，尚未匹配到用户此次操作。
- 公网 `/health/ready` 实际落在管理网页回退路由，返回 HTML，不作为 API 健康证据。

## 未解决证据缺口

用户说明发生于“提交审核”；仍需区分电脑审核页预览与手机扫码，以及点击时间，才能继续匹配首次身份识别失败。没有直接证据证明域名、微信配置或后端错误；不能把误提示修复等同于审核版本已能登录。

本轮未使用 Playwright、浏览器开发者工具或微信开发者工具；未做真机视觉/登录验收、线上配置修改、小程序上传、部署、推送或 PR。后续上传包含此修复的小程序版本前，继续追查审核环境首次识别失败并进行真实登录验收。
