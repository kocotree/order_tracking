# 一期版本发布记录

整理日期：2026-09-08。服务器标签与微信小程序版本分别管理；以下历史工作流证据来自仓库已有记录，本轮未重新查询 GitHub 或登录服务器。

## 已记录服务器版本

| 版本 | 提交／日期 | 变更与证据 | 验证边界 |
|---|---|---|---|
| v1.0.0 | `3e8832fc65541cc81bad8097b006d6b2c327a4bd` / 2026-09-08 | 服务器版本起点；标签核验 main CI 后自动发布镜像并部署。main CI `34174093619`、Release `34174536096` 成功，详见 [Issue #58 记录](issue-58-生产CD接入与首次部署.md) | 工作流成功；本轮未复查运行版本、外部登录和业务验收；小程序版本未核验 |

历史上曾使用 v1.0.6，随后用户确认服务器版本从 v1.0.0 开始；原始记录保留在 Issue #58 文档。不能把历史 v1.0.6 当作更晚版本，也不能复用已发布的 v1.0.0 标签。

## 已合并、生产归属待核验

本地 Git `3b2c90d` 可确认以下合并记录，不据此宣称对应生产发布完成。

| 变更 | 本地合并记录 | 影响与配套要求 |
|---|---|---|
| 明细合同出货时间（#61） | PR #62 / `43ce1a9` | 三端日期、导入、排序、逾期、提醒及合同导出；迁移 `20260908_0031`；需配套核验小程序版本。迁移将旧订单日期原值回填，不自动重新获取飞书历史订单 |
| 待导入加载优化（#63） | PR #64 / `3b2c90d` | 网页辅助请求与候选加载独立、候选列表数据库分页；业务范围和日期口径不变；本轮未复查该提交远程 CI |

正式订单重取、数据清理、真实消息和小程序上传／发布不是本表记录本身授予的操作。

## 后续版本记录模板

每次发布在此追加：版本与日期、精确 SHA、关联 Issue/PR、用户可见变化、迁移版本、配套小程序版本、main CI 与 Release 证据、运行版本及健康检查、真实验收结果、已知问题和上一可回滚版本。未验证项写“待核验”，不填推测结果。

完整缺陷继续由 GitHub Issues 跟踪，本文件只保留该版本的影响摘要，避免复制一份持续漂移的问题台账。
