# 跟单管理系统一期里程碑与决策记录

## 当前快照（2026-09-08）

- 一期正式需求规则更新至 V1.60：工厂／SKU明细合同出货时间改造由[Issue #61](https://github.com/kocotree/order_tracking/issues/61)集中跟踪，本轮仅文档与Issue，尚未实施。此前V1.59功能交付状态：S01–S11、按箱收货核对、返修发回服务器草稿及生产 CD 已进入 main；一期继续做验收和反馈迭代。
- main 与服务器标签 v1.0.0 的提交均为 `3e8832fc65541cc81bad8097b006d6b2c327a4bd`。[main CI](https://github.com/kocotree/order_tracking/actions/runs/34174093619) 五项任务通过；[Release](https://github.com/kocotree/order_tracking/actions/runs/34174536096) 的 CI 核验、两镜像发布与生产部署任务通过。此为工作流证据，未在本轮重新登录服务器核对健康或执行真实用户验收。
- #29 已由 [PR #36](https://github.com/kocotree/order_tracking/pull/36) 合并且 Issue 已关闭，不再是本工作区未提交任务。#44 已由 [PR #48](https://github.com/kocotree/order_tracking/pull/48) 合并且 Issue 已关闭；功能规则以需求第 4.3 节为准。
- 二期开始需求建设：内部同事飞书机器人登记替代仓库人员路线；周出货只回填现有 Base，取消工厂/SKU周汇总导出；差异登记影响订单进度，月账单暂只汇总数量；工厂分析、库存预警按[建设计划](../../二期/project/二期需求建设计划.md)进一步明确口径。方向确认不等于二期功能开发批准。
- 下方按日期保存的本地停止点、未提交说明及当时验证结果均为历史快照；不能直接作为当前 Git 状态或待执行指令。

## 1. 这份文件管什么

本文件只回答三个问题：

1. 项目现在做到哪一步；
2. 下一步做什么，做到什么算完成；
3. 哪些过程决策会影响后续阶段。

它不重复业务需求、Agent 操作规则或具体实现方案：

- 业务范围和验收标准：`docs/一期/requirements/一期需求文档.md`
- Agent 执行规则：`AGENTS.md`、`CLAUDE.md`
- 页面交互：已批准的原型及确认记录
- 技术实现：已批准的 `docs/一期/project/一期技术设计方案.md` V1.0
- 单次开发范围：当前工单

发生冲突时，业务行为以已批准的需求文档为准；页面交互以已批准的原型为准；技术实现以已批准的技术设计为准。对话、Agent 记忆和未确认草稿不能覆盖正式资料。

## 2. 当前状态

更新时间：2026年9月8日。当前结论见文件顶部；本节版本演进和阶段记录保留历史语境。

已经完成的历史决策：

- 一期需求 V1.10 已确认：工厂合同资料、合同编号和加工合同 Excel 导出纳入一期；只有已发数量为 0 的订单可以导出加工合同；系统不保存加工单价和合同金额；
- 一期需求 V1.11 已确认：最高管理员是管理员角色内的系统权限标记，只有最高管理员审核管理员申请及管理普通管理员启停；普通管理员和最高管理员均可审核工厂用户、绑定工厂及管理工厂用户启停；
- 一期需求 V1.12 已确认：当时管理员职位只使用“生产部、跟单人员”；管理员申请职位字段及其权限含义已由 V1.36 取消，订单业务字段“跟单人员”仍保留；
- 一期需求 V1.13 已确认：工厂用户职位只使用“老板、工厂员工”；当时管理员申请示例使用跟单人员姓名，管理员申请姓名来源与职位字段已由 V1.36 调整；
- 一期需求 V1.14 已确认：工厂用户申请增加验证联系电话，申请详情采用弹窗；待审核申请在弹窗内通过或拒绝，拒绝原因和审核记录必须保留；
- 一期需求 V1.15 已确认：工厂资料增加供应商编号，管理员网页端工厂列表在工厂名称前展示；供应商编号与生成合同编号使用的工厂代码分开保存；
- 一期需求 V1.16 已确认：工厂详情、编辑和新增弹窗统一增加供应商编号；新增时编号必填、唯一并转为大写，编辑时只读；
- 一期需求 V1.17 已确认：增加“质检单退回”和“按已收货发货单退回”两种返修单来源；当时设定工厂可分批填写发回数量及其中修好数量，管理员确认实际收回，返修数据不影响正常订单进度；该后续流程已由 V1.18 拆分修正；
- 一期需求 V1.18 已确认：质检单返修改用“返修数量＋报废数量＝发回实物总数量”，报废实物仍须返回仓库；按发货单退回不生成返修任务，而是同时扣减原订单已发数量和实际收到数量，由工厂按普通发货流程补发；当时设定已完成订单自动恢复为发货中，该状态名称已由 V1.35 替代；
- 一期需求 V1.19 已确认：“返修退回”页签只管理质检单返修；按发货单退回改为已收货发货单详情内的弹窗操作，退回原因必填，不生成独立单号、列表记录或状态，但必须在原发货单和订单日志中留痕；
- 一期需求 V1.20 已确认：当时取消返修单列表状态筛选和状态列，改用工厂多选、退回时间范围筛选及退回时间列，并沿用其他管理列表的分页；状态筛选和状态列已由 V1.35 恢复；
- 一期需求 V1.21 已确认：返修详情顶部只汇总工厂、退回总数量、返修数量和报废数量；工厂发回记录增加发货时间与产品规格明细字段；
- 一期需求 V1.22 已确认：仓库质检单数量改称仓库退回数量，退回总数量改为返修数量与报废数量的累计合计；工厂提交发回记录后立即生效，不再由管理员确认收回；
- 一期需求 V1.23 已确认：返修详情右上角显示工厂名称，数量汇总改为两行两列；工厂发回记录将行合计命名为返回数量，并增加对应规格仓库退回数量；质检明细相同箱号合并展示；
- 一期需求 V1.24 已确认：工厂累计发回统一称“返回总数量”，仓库最初发给工厂统一称“仓库退回总数量”；返修列表同步调整列顺序、日期展示和分页信息；
- 一期需求 V1.25 已确认：返修单级汇总使用“仓库退回总数量”，质检单和工厂发回记录的产品明细使用“仓库退回数量”；
- 一期需求 V1.26 已确认：只有已收货发货单在发货明细标题栏显示退回按钮；弹窗选择规格并展示实际收到与可退数量，填写本次退回数量和共用必填原因，不显示数量影响提示；
- 一期需求 V1.27 已确认：退回弹窗保持居中，删除发货单号、关联订单和工厂信息行，标题下直接展示退回明细；
- 一期需求 V1.28 已确认：管理员小程序底部“待收货”改为“发货”，内部包含“待收货”和只读“返修进度”；质检单返修通知可以进入对应详情，但小程序不提供新建返修任务或确认收回操作；
- 一期需求 V1.29 已确认：每张订单只存在一个合同出货时间；正常发货单由工厂提交后立即形成有效发货记录并计入订单累计发货数量，三端均不设置待收货、已收货、实际收到数量、到货日期或异常收货流程；管理员小程序“发货”内部改为只读“发货记录、返修进度”；按发货单退回只扣减累计有效发货数量；
- 一期需求 V1.30 已确认：管理员小程序统一为只读移动查询入口，订单详情不显示完成条件提示或“确认订单完成”按钮；订单完成仅在管理员网页端操作；
- 一期需求 V1.31 已确认：待导入订单页只使用“待处理、已导入”两个页签；待处理候选订单可以整单删除，删除只排除本系统候选记录、不删除飞书源数据，后续同步不再重复出现；取消“忽略、恢复、已忽略”流程；
- 一期需求 V1.32 已确认：订单不再保留“已取消”状态；未产生有效发货记录的正式订单由管理员直接删除，删除后不再显示订单及相关工厂任务，但保留删除操作日志；
- 一期需求 V1.33 已确认：只有已完成返修单可以由管理员归档；归档后二次确认并从管理员网页端、管理员小程序和所属工厂小程序隐藏，不增加已归档页签或恢复入口，返修数据和归档日志继续保留；
- 一期需求 V1.34 已确认：所属工厂可以在返修任务详情查看或下载原始质检附件，其他工厂不可访问；详情产品规格表不重复展示原质检箱号和次品原因；
- 一期需求 V1.35 已确认：三端正式订单状态统一为“未完成、已逾期、已完成”，返修状态统一为“未完成、已完成”；订单是否已产生发货记录继续通过数量与进度展示，返修不设置截止日期或逾期状态；
- 一期需求 V1.36 已确认：管理员网页端一期先作为独立网站提供“通过飞书登录”；管理员只在网页端申请，直接填写手机号并通过短信验证码验证，不填写职位；普通管理员默认查看全部业务数据；管理员小程序首次进入时授权手机号，与网页申请唯一匹配后绑定同一内部userId，未匹配时提示前往网页端申请；
- 一期需求 V1.37 已确认：工厂小程序“发货记录”只展示正常订单发货记录，不再设置返修页签；质检单返修历史发回记录统一从对应返修任务详情查看；
- 一期需求 V1.38 已确认：管理员小程序默认使用微信头像并允许仅在小程序端更换，不覆盖网页端飞书头像；“退出登录”只清除当前会话，不解除微信、手机号与内部userId绑定，重新登录不重复授权手机号；
- 一期需求 V1.39 已确认：取消每5分钟及其他定时自动发现飞书新订单，管理员在“待导入订单”页手动点击“获取飞书新订单”；获取只形成待处理候选，确认导入后生成草稿订单，发布后才成为工厂可见、可执行的正式订单；
- 一期需求 V1.40 已确认：同日同厂第一份合同使用无后缀基础编号，后续首次生成的第二份、第三份依次使用“-1”“-2”等后缀，已保存和已导出的编号永不重排或修改；
- 一期需求 V1.41 已确认：工厂委托代理人、开户银行、银行账号和来源别名不录入、不保存；飞书统一工厂名称直接严格匹配系统工厂，加工合同模板中已取消资料的对应位置保持空白；
- 一期需求 V1.42 已确认：聚水潭产品同步只纳入商品分类精确等于“童帽春夏、童配春夏、童装春夏、童帽秋冬、童配秋冬、童装秋冬”且来源状态为启用的产品和规格；已同步资料后续被停用或移出分类时标记为不可用于新订单，不删除历史记录；
- 一期需求 V1.43 已确认：管理员网页申请不再手工填写手机号或使用短信验证码，改用飞书企业通讯录返回的已验证手机号；管理员小程序仍使用微信授权手机号，只有与飞书手机号一致且唯一时才绑定同一内部 `userId`，普通管理员申请仍由最高管理员审核；
- 一期需求 V1.44 已确认：网页端和小程序端首次登录或绑定后使用短期访问凭证与最长30天未活动失效的滚动续期凭证自动恢复会话；刷新、关闭重开和正常服务重启不要求重新登录，主动退出、账号停用、客户端数据清除或续期凭证失效后不得恢复旧会话；
- 一期需求 V1.45 已确认：至少一个颜色/规格原始发货进度严格小于50%的订单可整单导入；逐派工保存不可降低的初始已发数量，统一已发数量为初始基线加系统有效发货数量；订单日期可空且不参与候选校验或页面展示；导入写入订单操作日志；带初始已发数量的订单不能导出加工合同；
- 一期需求 V1.46 已确认：发货清单按用户更新模板只保留“发货明细、汇总”，汇总按发货日期、产品名称和颜色/规格跨订单合并；退回弹窗只读展示原订单编号；撤回申请不设时限，已有退回仍可申请但管理员只能拒绝，待处理期间禁止新增退回；
- 一期需求 V1.47 已确认：S09 质检 Excel 只解析工厂、产品规格、数量、箱号和次品原因等结构化字段；原始 Excel 完整保留供授权查看或下载，系统不解析、单独展示、保存或补传照片；
- 一期需求 V1.48 已确认：管理员网页返修列表的单号去除按钮底纹边框、状态去除前置圆点并铺满表格；返修详情顶栏标题沿用通用样式，概览区只保留工厂、不重复显示状态，质检明细使用 `40px` 行高并合并相邻相同次品原因；
- 一期需求 V1.49 已确认：管理员小程序“发货”保留“发货记录、返修进度”两个页签；返修详情取消重复的结构化质检明细，改为查看或下载原始质检 Excel，并只读展示与工厂小程序同层级的工厂发回记录；不提供上传、提交发回或确认收回操作；
- 一期需求 V1.50 已确认：管理员和工厂小程序均不提供通知设置或类别开关，角色对应系统通知默认开启；微信未授权或拒绝只影响外部提醒，不影响站内通知中心；合同出货提醒固定为 D-10、D-5、D-3、D0，对应跟单人员接收全部节点，两名特别关注管理员只接收 D-3，到期后不再重复提醒；
- 一期需求 V1.51 已确认：合同出货提醒继续生成管理员站内通知，但外部提醒只通过飞书发送，不使用微信订阅消息；工厂用户的新订单、新返修和业务处理结果继续使用小程序订阅消息，微信只作为额外提醒且一次授权不等于永久可发送；
- 一期需求 V1.52 已确认：已启用工厂账号首次登录成功后显示一次可跳过的“开启微信提醒”引导，用户主动点击后才调用微信原生订阅接口；选择暂不开启不影响登录和业务使用，后续仍可从“我的”订阅；一次性订阅按每次授权机会记录，不描述为永久开关；
- 一期需求 V1.53 已确认：取消工厂首次登录后的自建微信提醒引导，登录不会自动取得订阅权限，两类小程序保留“我的—微信提醒授权”入口；
- 一期需求 V1.54 已确认：已启用工厂账号点击“微信授权登录”并完成账号状态确认后，调用微信原生订阅接口，由微信底部授权面板让用户选择；允许、拒绝或关闭均不阻断登录，非启用账号不触发，“我的”继续提供再次请求入口；
- 一期需求 V1.55 已确认：已启用管理员账号登录时同样调用微信原生订阅接口，请求正常发货和质检单返修模板；管理员的合同出货提醒只走飞书而不请求微信模板。管理员与工厂的原生授权结果均不阻断登录，两端“我的”均保留再次请求入口；
- 一期需求 V1.56 已确认：合同出货 D-10、D-5、D-3、D0 同时通知订单所涉工厂全部已启用用户，工厂端保留站内通知并通过微信订阅消息外发；跟单人员及特别关注管理员继续只通过飞书外发，特别关注管理员确定为“煎饼”和“核桃”；工厂四类业务通知映射到不超过 3 个微信模板 ID，优先共用字段兼容的订单提醒模板；运维告警通过飞书私聊发送给煎饼，飞书自身失败时保留数据库状态和服务器日志；
- 一期需求 V1.57 已确认：管理员网页端用户完成飞书授权并由服务器取得有效已验证手机号后，自动获得普通管理员权限，不再提交管理员申请或等待最高管理员审核；最高管理员仍只由生产环境预置或受控配置授予，既有停用管理员不能通过重新授权自行恢复；管理员小程序改为按手机号唯一匹配网页端飞书登录已建立的内部管理员；
- 一期需求 V1.58 已确认：最高管理员与普通管理员使用同一飞书 OAuth 入口；飞书唯一标识命中服务端受控白名单时直接获得最高管理员标记，不再单独初始化用户；普通管理员仍可审核工厂用户申请、绑定工厂及管理工厂用户启停；
- 管理员小程序正常发货单详情已确认：移动端只读展示发货概览、按订单默认折叠的发货明细、按箱号默认折叠的装箱明细、凭证、工厂备注和操作记录；发货明细订单卡片展示订单编号、规格数和合计数量，装箱明细产品名称后展示订单编号，不展示产品图片、产品编码或管理操作；
- 管理员小程序发货记录列表已确认：按订单编号或产品名称搜索，按工厂单选和发货日期范围筛选；列表卡片标题直接展示产品名称和订单编号，不单独展示关联订单，辅助展示工厂、发货单号、发货日期、发货数量和总箱数，点击进入只读发货单详情；
- 管理员小程序返修进度列表与详情已确认：列表只按工厂名称搜索，按工厂单选和退回日期范围筛选；详情顶部只展示返修数量、报废数量和返回进度，不重复展示结构化质检明细，改为查看或下载原始质检 Excel，并按发回日期分层折叠展示工厂发回记录；
- 管理员小程序“我的”页面已确认：头像区只展示微信头像、姓名和管理员类型并删除装饰圆环；下方无标题信息块连续展示姓名、管理员类型、脱敏联系电话、微信提醒授权和通知中心，各行只用细分割线区分；通知中心右侧显示数字未读圆圈和进入箭头；不提供通知设置或类别开关，三类管理员系统通知默认全部开启；提供不解除账号绑定的退出登录，不开放其他资料编辑或解除绑定；
- 工厂小程序“我的”页面已确认：头像区只展示微信头像、姓名和职位并删除装饰圆环；下方无标题信息块连续展示姓名、脱敏联系电话、职位、所属工厂、微信提醒授权和通知中心；通知中心右侧显示数字未读圆圈和进入箭头；不提供通知设置或类别开关，新订单任务、合同出货提醒、新返修任务和业务处理结果四类工厂系统通知默认全部开启；底部导航不显示通知红点；
- 工厂小程序返修入口已确认：返修只保留在“任务—返修”及对应详情中；“发货记录”不设置返修页签，底部导航仍为“任务、发货记录、我的”；
- 三端任务状态已确认：订单筛选、列表和详情只使用“未完成、已逾期、已完成”，返修业务只使用“未完成、已完成”；管理员网页列表仍显示状态，详情概览区按 V1.48 不再重复状态标签；草稿、发货单和审核等其他业务对象状态不变；
- 一期三端页面地图已确认，网页端与两类小程序的一级导航、页面层级和职责边界已经确定；
- 一期业务范围、终端职责、MySQL 单一正式数据源和新建独立代码项目已经确认；
- 公司 ECS 的 Ubuntu 22.04 和 MySQL 8.0.46 已完成只读核验；
- 管理员网页端视觉基准已改为用户提供的聚水潭 ERP 参考：亮蓝色导航、浅灰工作区、纯白内容块和紧凑后台信息密度；
- 管理员网页端低保真原型已同步 V1.29：删除正常收货相关页面和数据，发货单提交即显示“已发货”，退回按有效发货数量计算。
- 管理员网页端、管理员小程序端和工厂小程序端低保真原型已经全部确认；V1.36 至 V1.38 的登录、跨端绑定、头像和退出登录规则均已落实到对应原型，V1.39 已补齐管理员网页端手动获取飞书新订单的低保真交互。
- 一期技术设计方案 V1.0 已整体确认：架构、技术栈、数据模型、API、身份权限、外部集成、部署、备份、监控、测试与合同编号并发生成规则已经确定；
- 一期开发计划 V1.0 已整体确认：一期按 S00 至 S12 十三个切片实施，S00 至 S04 默认串行，后续满足依赖后最多同时执行两个独立 Worktree 工单；
- S00“Git 与工程基线”已完成：V1.40 正式资料基线、三端正式目录、MySQL 8 开发与测试环境、后端和 worker 通用基础设施、锁文件、容器构建及 CI 检查均已建立并通过本地验收；

需要特别区分：V1.5 管理员端页面地图和原型已经失效，只有视觉方向可以继续参考；当前三端页面地图已经按 V1.30 更新。管理员网页端原型已于 2026 年 8 月 18 日全部确认完成。

V1.40 正式资料基线和 S00 工程基线已通过快进方式合并到 `main`；后端、管理员网页、小程序和 CI 保持独立、可回退的提交。

S01 已完成数据库、后端身份闭环、管理员网页端和单一管理员小程序工程实现，并以快进方式合并到 `main`；真实 MySQL 8 测试、三端检查与构建、OpenAPI 双端类型漂移和 Docker 镜像构建均通过。本机假适配器演示入口已补齐，Codex 已用实际浏览器和运行中 API 检查申请、审核、权限、跨端绑定及停用会话撤销，并修正 Uvicorn 访问日志暴露 OAuth 查询串的问题。用户已确认当前本地功能没有问题，图标和颜色等视觉细节可后续调整；微信开发者工具/真机检查和真实外部联调仍未完成，可在后续第二阶段补验。

S02 已完成本地代码实施和用户功能验收：精简工厂资料、工厂用户申请审核、启停、工厂隔离和单一小程序最小身份页均已落地；真实 MySQL 8、Node.js 24 三端检查、OpenAPI 类型、Docker 镜像和本地浏览器闭环通过。用户确认工厂保留编辑、不增加硬删除，已投入使用的工厂通过停用管理，图标、颜色和按钮样式可后续统一优化；S02 按授权快进合并到 `main`，真实微信开发者工具、手机号和真机联调待补。

### 当前阶段

一期功能实施和反馈迭代已推进至 V1.59，生产 v1.0.0 Release 工作流成功。产品图片展示已由 PR #3 补齐，旧记录“尚未展示真实图片”不再是当前缺口；历史同步中的无图/失败数字不代表今天的图片状态。

当前先按一期验收矩阵和开放 Issue 收口真实用户授权、真机/通知、持续备份告警与业务验收等证据。具体环境、备份恢复及发布例外见 [S12 环境记录](S12-环境盘点与部署记录.md)和 [Issue #58](issue-58-生产CD接入与首次部署.md)，不把上线工作流成功当作全部验收结束。

二期同步进行需求梳理，当前不新增业务字段、API、数据库迁移、页面或定时任务。下一步见[二期需求建设计划](../../二期/project/二期需求建设计划.md)。

## 3. 一期阶段路线与验收门（原计划保留）

下表保留一期原阶段路线及当时状态，不是当前待办清单；当前状态与下一步以文件顶部快照为准。阶段确认门继续适用。

| 步骤 | 要做什么 | 完成标准 |
|---|---|---|
| 1. 页面地图与管理员原型（已完成） | 明确三端职责和页面地图，制作管理员网页端低保真原型 | 核心管理流程可走通，用户已确认进入小程序原型阶段 |
| 2. 小程序原型（已完成） | 制作管理员端和工厂端低保真原型 | 两类角色的移动流程可走通，与网页端无重复或遗漏，用户确认进入技术设计 |
| 3. 技术验证与设计（已完成） | 验证身份映射、HTTPS API、Excel 下载、ECS 资源和环境隔离，完成架构、数据模型、接口、权限、部署和测试设计 | 需求均能映射到设计，关键风险有结论，用户批准技术设计 |
| 4. 开发计划与工单（已完成） | 按可独立演示的垂直切片安排文件、依赖、测试和验收 | 所有一期需求都有对应任务，不含二期功能，用户批准开发计划 |
| 5. Git 与工程基线（已完成） | 建立仓库基线、三端目录、开发和测试环境及基础检查 | 可以安全回退，基础构建和测试通过 |
| 6. 功能实施（当前） | 按工单逐个开发、测试和验收 | 每个切片均有可验证结果，核心权限、状态、数量、事务和 Excel 测试通过 |
| 7. 集成验收 | 使用多角色、真实 Excel、浏览器、微信开发者工具和真机走完整流程 | 一期验收标准全部有证据，遗留问题已关闭或明确接受 |
| 8. 试运行 | 选择两至三个工厂，只导入满足至少一个颜色/规格原始发货进度严格小于50%的完整订单 | 初始基线和后续发货口径正确，真实订单运行稳定，问题修复并复验通过 |
| 9. 正式切换 | 按统一日期接入之后的新增订单 | 生产检查、备份恢复、权限和回滚方案验证通过，用户批准上线 |

## 4. 过程决策

| 日期 | 决策 | 影响 |
|---|---|---|
| 2026-08-05 | V1.5 原型作废 | 页面地图、字段和交互全部按当前 V1.7 需求基线重做；当时暂留的视觉方向已于 2026-08-12 被新的 ERP 参考图取代 |
| 2026-08-05 | 三端页面地图先统一检查，再分别制作详细原型 | 先消除网页端、管理员小程序端和工厂小程序端的职责重复与遗漏 |
| 2026-08-05 | 原型阶段允许必要的只读调查和小型技术验证 | 可以提前发现不可行假设，但不得越过确认门开始正式实现 |
| 2026-08-12 | 多维表订单按已核对的实际字段导入；一个订单只指定一名跟单人员 | 使用“出货总数”和“未出数量”判断首次导入范围；跟单人员不一致时阻止导入；不导入同名公式交期及裁剪、绣花、印花等生产进度字段 |
| 2026-08-12 | 一期增加按“一个订单＋一个工厂”导出加工合同 Excel（此决策先于8月13日暂缓，又由8月14日恢复并收窄） | 最终范围以2026-08-14决策为准 |
| 2026-08-13 | 合同相关能力暂缓（已由2026-08-14决策调整） | 本条保留为历史记录，不再作为当前一期范围依据 |
| 2026-08-12 | 管理员网页端把订单管理和发货收货合并为同一管理入口 | 管理员在订单上下文中查看发货并处理收货；创建发货单仍由工厂小程序端执行 |
| 2026-08-12 | 一期三端页面地图确认 | 网页端使用“订单看板、订单与发货、产品资料、工厂资料、人员管理”；管理员小程序使用“订单、待收货、我的”；工厂小程序使用“任务、发货记录、我的”；两类小程序均不设独立工作台 |
| 2026-08-12 | 管理员网页端“订单看板”只提供订单编号或产品名称关键词搜索 | 看板不增加“只看我的”、跟单人员和工厂筛选；所有授权管理员可查看全量订单，详细筛选留在“订单与发货”页面设计 |
| 2026-08-12 | 管理员网页端严格沿用用户提供的聚水潭 ERP 色系与后台视觉 | 公共外壳使用亮蓝应用栏＋可折叠白色页面菜单，内容区统一采用浅灰底、纯白块、细分隔线和亮蓝主操作；此约定不代表小程序端已经设计 |
| 2026-08-12 | 管理员网页端一级入口与二级页面分栏展示 | 五个一级入口固定放在亮蓝应用栏，可折叠白色栏只显示当前一级入口的二级页面 |
| 2026-08-12 | 订单看板使用“三个统计卡片＋最近通知”摘要布局 | 删除独立的“待处理事项”列表和“待审核作废申请”统计卡；最近通知放在三个统计卡片右侧，搜索只过滤下方进行中订单 |
| 2026-08-14 | 产品资料以聚水潭为唯一上游，sku_id作为页面产品编码 | 后端首次全量、后续按修改时间增量同步到MySQL；本系统不提供新增或修改产品及规格的入口，订单导入只匹配已同步资料 |
| 2026-08-14 | 工厂合同资料与加工合同 Excel 导出纳入一期，但系统不保存加工单价和合同金额 | 一份合同只对应一个订单和一个工厂；合同编号为“日期-KK-工厂代码”，同日同厂多份增加顺序号；文件名同时包含合同编号、订单编号和产品名称；导出表格保留空白加工单价列和金额公式，由管理员导出后填写 |
| 2026-08-14 | 只有已发数量为 0 的订单可以导出加工合同 | 已发数量大于 0 时禁止首次导出和重复导出，避免订单开始执行后继续生成或变更加工合同 |
| 2026-08-17 | 人员管理采用“最高管理员权限标记＋两种业务角色” | 只有最高管理员可以查看和审核管理员申请、管理普通管理员启停；普通管理员不显示管理员申请入口，可审核工厂用户并维护工厂用户启停；最高管理员不能通过普通申请获得 |
| 2026-08-17 | 人员职位采用固定值 | 管理员只使用“生产部、跟单人员”；原型跟单人员统一为烧麦、松子、橄榄、大葱、青椒 |
| 2026-08-17 | 工厂用户职位名称确定 | 工厂用户只使用“老板、工厂员工”；管理员申请示例使用跟单人员姓名 |
| 2026-08-17 | 管理员网页端低保真原型整体确认完成 | 进入管理员小程序原型设计；小程序继续按页面逐项确认，不复制网页端复杂录入、基础资料或 Excel 导出功能 |
| 2026-08-17 | 工厂用户申请改为详情弹窗审核 | 列表操作统一显示“详情”；弹窗核对真实姓名、验证联系电话、职位、申请工厂和工厂联系人，待审核申请在弹窗内通过或填写原因后拒绝 |
| 2026-08-17 | 工厂资料增加独立供应商编号 | 编号取自《成品工厂供应商信息》，工厂列表在工厂名称前展示并支持排序；不得覆盖合同编号使用的工厂代码 |
| 2026-08-17 | 工厂详情、编辑和新增弹窗增加供应商编号 | 弹窗按“编号与工厂代码、工厂名称与法定代表人、整行单位全称、联系人与联系电话、整行单位地址”排列；新增编号必填、唯一并转为大写，编辑只读 |
| 2026-08-17 | 一期增加独立的次品退回返修闭环 | 管理员网页端“订单与发货”增加“返修退回”；质检单来源不关联原订单，按发货单来源关联已收货明细；当时设定工厂分批填写发回数量和修好数量，管理员确认实际收回；返修发回记录不计入正常订单进度（已由下一条 V1.18 决策替代） |
| 2026-08-17 | 两种退回来源采用不同后续流程 | 质检单来源生成独立返修任务，工厂填写返修数量和报废数量且全部实物返回；按发货单来源同时扣减原订单已发数量与实际收到数量，通过原正常任务和普通发货单补发，不填写返修或报废数量；当时使用“恢复为发货中”，该状态名称已由 V1.35 替代 |
| 2026-08-17 | 返修页面与发货单退回入口分开 | “返修退回”只展示质检单返修任务；已收货发货单在详情弹窗中选择SKU、填写退回数量和必填原因；不生成独立单号或列表记录，只在原发货单和订单日志留痕 |
| 2026-08-17 | 返修单列表以退回时间替代状态列（已由 V1.35 调整） | 退回时间记录管理员上传质检单并确认创建返修任务的系统时间；V1.35 恢复“未完成、已完成”状态筛选、列表状态列和详情状态标签 |
| 2026-08-17 | 返修详情压缩汇总并展开工厂发回产品明细 | 顶部只展示工厂、退回总数量、返修数量和报废数量；发回记录按产品规格展示发货时间与数量；当时保留的实际收回和确认操作已由下一条 V1.22 决策取消 |
| 2026-08-17 | 质检单返修取消管理员确认收回 | 仓库质检单数量称为仓库退回数量；工厂提交的返修数量与报废数量立即累计为退回总数量，相等时自动完成；管理员网页端和小程序端均不再提供返修确认收回操作 |
| 2026-08-17 | 返修详情汇总与产品规格数量字段调整 | 右上角显示工厂名称；数量汇总按返修与报废、仓库退回与退回总量两行两列展示；发回明细使用返回数量并展示对应规格仓库退回数量；质检明细相同箱号合并展示 |
| 2026-08-17 | 区分工厂返回与仓库退回数量术语 | 工厂累计发回称返回总数量，仓库最初发给工厂称仓库退回总数量；返修列表按返修、报废、返回总量、仓库退回总量排列，退回时间只显示日期并补齐分页信息 |
| 2026-08-17 | 区分返修单汇总与产品明细的仓库退回数量 | 整张返修单使用仓库退回总数量；质检单明细及工厂发回记录中的产品规格数量使用仓库退回数量 |
| 2026-08-17 | 已收货发货单退回弹窗确认 | 退回按钮位于发货明细标题栏且仅已收货显示；弹窗勾选规格，展示实际收到与可退数量，填写本次退回数量及共用必填原因；不显示数量影响提示 |
| 2026-08-17 | 精简发货单退回弹窗 | 弹窗居中显示；删除重复的发货单号、关联订单和工厂信息行，标题下直接展示退回明细 |
| 2026-08-18 | 管理员网页端返修相关原型确认完成 | “返修退回”列表、新建、详情及已收货发货单退回弹窗均已确认；管理员网页端低保真原型至此全部完成，后续进入管理员小程序原型 |
| 2026-08-18 | 管理员小程序增加只读返修进度入口 | 底部“待收货”调整为“发货”，内部使用“待收货、返修进度”两个页签；待收货红点只统计正常发货待收货数量，返修进度只读展示质检单返修列表与详情，通知可直接进入对应详情 |
| 2026-08-18 | 正常发货改为提交即生效且取消收货确认 | 每张订单只保存一个合同出货时间；工厂提交正式发货单后立即计入累计有效发货数量；删除待收货、已收货、实际收到数量、到货日期和异常收货；管理员小程序“发货”改为只读“发货记录、返修进度”；按发货单退回只扣减累计有效发货数量；管理员网页端原型已同步 V1.29 |
| 2026-08-18 | 待导入订单取消忽略与恢复 | 页面只保留“待处理、已导入”；待处理候选订单操作列增加删除并二次确认，删除不影响飞书源数据，但保留排除标记避免后续同步再次出现；已导入订单只允许查看 |
| 2026-08-18 | 订单取消改为直接删除 | 订单不再设置或展示“已取消”状态；未产生有效发货记录的正式订单允许管理员直接删除，删除后不再出现在订单列表和工厂任务中，但删除操作日志必须保留 |
| 2026-08-18 | 已完成返修单支持归档 | 只有已完成返修单显示归档操作；二次确认后管理员网页端、管理员小程序和所属工厂小程序均不再展示，不增加已归档页签或恢复入口，返修数据、归档人、归档时间和操作日志继续保留 |
| 2026-08-18 | 管理员小程序取消订单完成操作 | 管理员小程序订单详情只读展示订单、工厂进度和关联发货单，不显示完成条件提示或确认按钮；订单完成仅在管理员网页端执行，管理员小程序不提供订单状态变更操作 |
| 2026-08-18 | 管理员小程序发货记录列表确认 | “发货”默认进入“发货记录”；支持订单编号或产品名称搜索、工厂多选和发货日期范围筛选；卡片以产品名称和工厂为主要信息，发货单号降为辅助信息，点击进入只读详情且不提供管理操作 |
| 2026-08-18 | 管理员小程序返修进度列表确认 | 只按工厂名称搜索，使用工厂多选和退回日期范围筛选；卡片展示数量和返回进度，只读查看且不提供新建、编辑或确认收回；详情页另行确认 |
| 2026-08-18 | 管理员小程序返修详情确认 | 顶部展示工厂、返修单号、退回日期、四项数量和返回进度；质检明细按产品折叠，工厂发回记录按日期折叠后按产品分组；不展示质检单附件、照片或任何管理操作 |
| 2026-08-18 | 管理员小程序“我的”页面确认 | 展示头像、姓名、管理员类型和账号状态，不展示职位或微信绑定状态；个人可开关新订单、临期逾期、正常发货和质检单返修通知，微信通知未授权时在对应行提示并提供开启入口；不提供资料编辑、退出登录或解除绑定 |
| 2026-08-18 | 管理员小程序移动查询层级优化 | 订单列表不显示默认排序说明；返修卡片以工厂为单位汇总产品并突出待返回数量；发货单箱号默认全部收起；“我的”不展示微信绑定状态，通知未授权时仅在对应业务行提示并提供开启入口 |
| 2026-08-18 | 管理员小程序查询与详情展示统一优化 | 订单只按订单编号或产品名称搜索，订单、发货记录和返修进度的工厂筛选全部改为单选；订单卡片整卡进入详情且不显示“查看详情”栏，订单详情工厂进度默认折叠；发货明细按订单、装箱明细按箱号默认折叠并精简重复字段；返修详情顶部精简为返修、报废和返回进度，规格返回关系改用进度条；“业务通知设置”改为“通知设置”并删除姓名旁的“账号正常”标签 |
| 2026-08-19 | 工厂小程序返修入口命名确认（已由 V1.37 调整） | 当时确认“任务”和“发货记录”均使用“订单、返修”页签；V1.37 后返修入口只保留在“任务—返修”及对应详情中 |
| 2026-08-19 | 工厂返修任务详情与质检附件权限确认 | 详情使用单页展示任务概览、产品规格进度、质检附件和历史发回记录；所属工厂可以查看或下载原始质检附件，其他工厂不可访问；规格表不重复展示原质检箱号和次品原因，数量术语继续使用“返修数量” |
| 2026-08-19 | 工厂任务状态展示精简（已由 V1.35 调整） | 当时确认“任务—订单”和“任务—返修”均只显示“未完成、已完成”；V1.35 后订单增加“已逾期”，返修仍保持两种状态 |
| 2026-08-19 | V1.35 统一三端订单与返修状态 | 正式订单只使用“未完成、已逾期、已完成”，未完成订单超过合同出货时间自动变为“已逾期”；返修只使用“未完成、已完成”，不设置截止日期或逾期状态；管理员网页端、管理员小程序和工厂小程序的筛选、列表、卡片与详情同步更新 |
| 2026-08-19 | V1.36 确认管理员登录、申请与跨端绑定 | 网页端独立运行时使用“通过飞书登录”，无角色用户只在网页端填写手机号和短信验证码申请且不填写职位；普通管理员默认查看全部业务数据；管理员小程序不提供申请入口，首次授权手机号后与网页申请唯一匹配并绑定同一内部userId，未匹配时提示先前往网页端申请 |
| 2026-08-19 | V1.37 收窄工厂发货记录入口 | 工厂小程序“发货记录”只展示正常订单发货记录，不设置返修页签；返修历史发回记录统一在对应返修任务详情内查看，避免与返修任务形成重复入口 |
| 2026-08-19 | V1.38 确认管理员小程序头像与退出登录 | 小程序默认使用微信头像并允许仅在小程序端更换，不覆盖网页端飞书头像；退出登录经二次确认后只清除当前会话，不解除微信、手机号与内部userId绑定，重新登录恢复原账号且不重复授权手机号 |
| 2026-08-19 | 三端低保真原型全部确认完成 | 管理员网页端、管理员小程序端和工厂小程序端核心流程均已确认，阶段 2 完成，项目进入技术验证与设计；正式编码和工程脚手架仍须等待技术设计批准 |
| 2026-08-20 | V1.39 改为管理员手动获取飞书新订单 | 一期取消每5分钟及其他定时自动发现；管理员在现有“待导入订单”页点击“获取飞书新订单”，增量获取尚未导入、尚未排除的来源记录并形成待处理候选，反馈新增、已存在或跳过、失败数量及上次成功获取时间；重复点击保持幂等，失败不影响已有草稿或正式订单 |
| 2026-08-20 | 统一候选、草稿与正式订单口径 | 待处理候选订单只是飞书来源快照；确认导入后生成工厂不可见的草稿订单；草稿完成派工并发布后才成为工厂可见、可执行的正式订单；“已导入”不等同于已发布 |
| 2026-08-20 | V1.40 确认合同编号稳定顺序 | 同日同厂第一份合同使用无后缀基础编号，第二份使用“-1”，第三份使用“-2”并依次增加；按首次成功生成编号的顺序分配，既有编号永不重排或修改 |
| 2026-08-20 | V1.41 精简工厂资料 | 委托代理人、开户银行、银行账号和来源别名不在页面录入、不在数据库保存；飞书统一工厂名称直接严格匹配系统工厂，名称不一致时修正上游数据；联系人规则不变 |
| 2026-08-20 | 一期技术设计方案 V1.0 整体确认 | 阶段 3 完成，项目进入“开发计划与工单”；开发计划再次确认前不创建正式工程脚手架、不开始功能编码 |
| 2026-08-20 | 一期开发计划 V1.0 整体确认 | 阶段4完成，项目进入“Git与工程基线”；一期按 S00 至 S12 实施，一个开发工单对应一个 Codex 对话和独立 Worktree，S00 建立 Git 基线前不并行开发 |
| 2026-08-20 | S00 Git 与工程基线完成 | V1.40 资料基线和三端独立工程已提交；真实 MySQL 8、后端/worker 基础设施、容器、锁文件、CI、管理员网页浏览器检查和微信开发者工具编译均通过，项目进入阶段 6，下一步先编写并确认 S01 工单 |
| 2026-08-20 | S00 工程基线合并到 `main` | 清理并忽略微信开发者工具生成的 JavaScript 产物后，以快进方式将完整 S00 提交链纳入 `main`；后续 S01 分支和 Worktree 必须从该基线创建 |
| 2026-08-20 | S01 本地功能验收通过，真实联调待补 | 独立分支已完成数据库、后端、管理员网页和单一小程序工程；假适配器本机演示、真实 MySQL 8 自动测试、Codex 网页浏览器检查及运行中跨端会话检查通过，用户确认当前本地功能没有问题且视觉细节可后续调整；真实飞书、短信、微信手机号、私有头像存储和微信开发者工具/真机检查仍待第二阶段验收，未收到指令前不合并或启动 S02 |
| 2026-08-20 | S01 快进合并到 `main`，启动 S02 工单确认 | 用户同意先合并 S01 并允许后续补验视觉和真实外部条件；S02 先按已批准需求、原型、技术设计和开发计划形成正式工单，确认后再在独立分支和 worktree 实施 |
| 2026-08-20 | S02 工单确认并开工 | 用户确认精简工厂资料和审核通过后的最小身份页；S02 在 `codex/s02-factory-users` 独立 worktree 从真实 MySQL 8 工厂唯一约束开始垂直切片 TDD |
| 2026-08-20 | S02 本地实施完成，待用户验收和真实微信联调 | 工厂资料、申请审核、用户启停、权限隔离和单一小程序最小身份页已实现；真实 MySQL 8、Node.js 24 三端检查、OpenAPI、Docker 与本地浏览器闭环通过；真实微信手机号、开发者工具和真机尚未验收，分支未合并 |
| 2026-08-20 | S02 本地验收通过并授权合并 | 用户确认当前功能没有问题；工厂保留编辑、不增加硬删除，已使用工厂通过停用管理，视觉细节后续统一优化；S02 快进合并到 `main`，真实微信手机号、开发者工具和真机联调待补，S03 未启动 |
| 2026-08-21 | V1.42 收窄聚水潭产品同步范围并启动 S03 工单 | 用户确认只同步“童帽春夏、童配春夏、童装春夏、童帽秋冬、童配秋冬、童装秋冬”六个商品分类中来源状态为启用的产品；只读真实查询已确认聚水潭 SKU 响应包含商品分类和启用状态，正式 OpenAPI 的分类请求、分页、限频和增量语义仍待受控联调；S03 只编写并确认工单，确认前不编码 |
| 2026-08-21 | S03 工单确认并开工 | 用户确认按已批准工单实施；同步领域服务、真实 MySQL 8 迁移与测试、worker、管理员只读 API 和网页按已确认接缝执行，外部条件缺失时只使用显式可控假适配器并保留真实联调缺口 |
| 2026-08-21 | S03 本地实施完成，待用户验收和真实联调 | 六分类精确筛选、启停与进出范围、失败不推进游标、图片重试、只读 API 和网页列表已通过真实 MySQL 8 和本地浏览器验收；本地演示只使用假产品，正式聚水潭 OpenAPI 字段、分页、限频、增量边界和 MinIO 仍待受控联调，分支未合并 |
| 2026-08-21 | 正式前端实施增加强制原型对照门 | 后续工单必须实际打开对应已确认原型并读取相关组件和样式，正式页面逐项对齐外壳、信息层级、字段、控件、交互和响应式布局；优先移植原型资产、设计变量和组件结构，但不得提前开放后续工单功能，完成前必须用实际浏览器并排检查并记录有意差异 |
| 2026-08-21 | S01 至 S03 管理员网页补做原型对照 | 复用原型 logo、导航图形、设计变量、双层后台外壳、列表筛选、排序、分页和弹窗结构；修正 S02 工厂页此前缺少的详情、重置、排序和分页，并把 S03 产品页调整为独立搜索卡片和只读列表卡片；管理员网页自动化与构建、小程序回归和本地浏览器复验通过，微信开发者工具与真机视觉仍待用户确认 |
| 2026-08-21 | S03 本地验收通过并授权合并 | 用户确认现阶段不进行真实外部联调；小程序身份页按已确认原型完成逐页微调，微信开发者工具只读预览通过，所有重复自绘右上角胶囊已移除且只保留微信原生胶囊；真实聚水潭、MinIO、微信手机号和真机联调后补，不阻塞按“本地验收通过、真实联调待补”合并 S03 |
| 2026-08-21 | S03 快进合并到 `main` | `codex/s03-product-sync` 经最终真实 MySQL 8、Node.js 24、OpenAPI、Docker、敏感信息和差异检查后快进合并到 `main`；S03 分支保留真实聚水潭与 MinIO 联调缺口，下一步允许设计 S04 工单但不自动实施 |
| 2026-08-21 | 开始编写 S04 正式工单草案 | 只设计草稿、派工、发布、撤回、无有效发货订单删除、完成与撤销完成，以及三端订单查询；订单级分类、数量文案和原型未画出的状态操作仍待用户确认，确认前不创建业务分支、不编码 |
| 2026-08-21 | S04 工单确认并开放实施门 | 用户确认取消无正式定义的订单级分类、统一“已发/未发”数量文案、补齐管理员网页端订单状态操作、S07 前隐藏无效创建发货入口，并接受 S04 只验收无发货守卫接缝；“确认订单完成、撤销完成”原型交互已由 `b12d558` 保存，下一步允许在独立分支和 worktree 实施 S04 |
| 2026-08-21 | S04 本地业务闭环验收通过并合并 | 草稿、派工、发布、撤回、删除、完成/撤销完成、三端查询、工厂隔离和发件箱已落地；真实 MySQL 8、OpenAPI、Node.js 24 三端检查、管理员网页浏览器闭环和微信开发者工具四页视觉预览通过。预览中发现并修复两个详情页返回事件未绑定；S04 已快进合并到 `main`，真机、真实发货守卫、通知发送和生产联调继续后补，不启动后续工单 |
| 2026-08-21 | 追加确认 S04 看板分类和排序口径 | 看板恢复“分类”列：童装春夏/秋冬映射为“服装”，童帽与童配春夏/秋冬映射为“帽子”，混合订单显示两个标签，缺失显示“—”；分类及其余业务列支持表头升降序，序号不排序；看板搜索只匹配订单编号和产品名称 |
| 2026-08-21 | 追加确认 S04 订单列表严格对齐原型 | 状态和全部筛选条件常驻显示；恢复分类筛选和列，工厂及跟单人员多选，完整查询结果表头排序后分页；暂不显示“更多操作”和手工新建入口，既有路由和后端能力保留待后续确认开放 |
| 2026-08-22 | 固化管理员网页端表格视觉规范 | 表格卡片标题行统一为 54px 高、`0 16px` 内边距和 `18px / 800` 标题；表头固定 40px；序号列统一为计算后 52px、左右各 8px、居中、单行且不排序；各业务表保留已确认字号、数据行高度、最小宽度和列宽，并把完整规范加入 Codex 与 Claude Code 的强制读取入口 |
| 2026-08-24 | S05 本地验收后合并到 `main` | 用户确认合并；`fedeb1c` 完成 S05 实现提交，`ef844f3` 将其合并到 `main`。保留真实飞书企业应用只读联调、真实候选导入和生产环境验证缺口，不启动 S06 |
| 2026-08-24 | V1.43 取消管理员申请短信验证 | 管理员网页端使用飞书 OAuth 返回的企业通讯录已验证手机号，页面只展示脱敏号码并直接提交申请；管理员小程序继续取得微信授权手机号，只有与飞书号码一致且唯一时绑定同一内部 `userId`；飞书缺少手机号权限、号码缺失、冲突或重复时均禁止自动创建或合并账号；普通管理员仍须最高管理员审核 |
| 2026-08-24 | V1.44 确认网页与小程序持久登录 | 首次登录或绑定后通过短期访问凭证和最长30天未活动失效的滚动续期凭证自动恢复会话；网页和小程序并发401只发起一次续期；退出、停用、权限撤销、客户端数据清除或续期失效后旧会话不可恢复；本次作为S01修复完成后再开始编写S06工单 |
| 2026-08-25 | S06 本地验收后授权合并 `main` | 加工合同 Excel、稳定编号、合同快照、私有文件和管理员网页导出闭环完成；用户已检查页面和模板并授权合并。真实外部联调继续后补；部分发货订单导入改造另开独立任务，S07 不自动启动 |
| 2026-08-25 | V1.45 确认部分发货订单导入与初始已发数量 | 至少一个颜色/规格原始发货进度严格小于50%时整单导入全部明细；初始已发数量与系统有效发货分开保存并形成统一数量口径；订单日期可空且不展示；订单详情底部展示最小操作日志；S06 合同资格继续要求统一已发数量为0；本改造在独立 worktree 实施，不启动 S07 |
| 2026-08-25 | V1.45 部分发货订单导入改造合并 `main` | `be9be44` 完成主要实现，`53cfda6` 修复列表上下文，`06010f4` 合并到 `main`；S07 的初始已发与统一数量口径前置已满足 |
| 2026-08-25 | V1.46 与 S08 工单确认 | 发货清单采用更新模板并只保留“发货明细、汇总”，汇总按日期、名称和颜色/规格跨订单合并；退回自动归属原发货订单并只读展示订单编号；管理员详情增加撤回申请卡片；撤回不设时限，已有退回仍可申请但只能拒绝，待处理期间禁止新增退回；S08 实施门开放 |
| 2026-08-26 | V1.47 收窄 S09 质检照片范围 | 质检 Excel 只解析工厂、产品规格、数量、箱号和次品原因等结构化字段；原始 Excel 完整保留供管理员和所属工厂查看或下载；系统不解析、单独展示、另行保存或补传照片，返修新建和详情原型同步移除照片列与入口 |
| 2026-08-26 | V1.48 收口管理员网页返修表格 | 列表单号使用无底纹无边框文字链接、状态不显示前置圆点，表格铺满可用宽度；详情顶栏标题使用通用样式，概览区不再重复状态标签，质检明细统一 `40px` 行高并合并相邻相同次品原因 |
| 2026-08-27 | V1.49 调整管理员小程序返修详情 | “发货”保留发货记录与返修进度双页签；返修详情不重复展示结构化质检明细，改为查看或下载原始质检 Excel，并只读展示与工厂小程序同层级、默认折叠日期批次的工厂发回记录；管理员小程序不提供上传或返修发回操作 |
| 2026-08-25 | S08 本地实现完成，等待验收 | 作废申请与审核、按原发货行退回、负向台账、普通补发口径、两 Sheet 发货清单和三端页面已实现；S08 定向测试及三端构建通过，管理员网页已实际检查；微信开发者工具、真机、真实 Excel/WPS 和生产环境仍未验收，未经用户授权不合并 `main` |
| 2026-08-25 | 开始编写 S07 正常装箱发货闭环工单 | 当前只设计发货草稿、装箱、正常提交、数量台账和三端只读结果；装箱组、多发显式归属、服务端草稿恢复及页面范围等待用户确认，确认前不创建业务分支、不编码 |
| 2026-08-25 | S07 工单确认并开放实施门 | 用户确认完整工单、装箱组连续箱复制、多发显式归属、服务端草稿恢复及四端页面范围；正式页面严格复刻原型，只有正式需求、真实数据或微信平台冲突时采用最小调整并记录，不并行启动其他工单 |
| 2026-08-25 | S07 已合并并开始 S08 工单 | S07 正常发货工作流合并为 `9a02ef4`；S08 实施前必须确认发货清单模板、管理员作废审核、多订单同规格退回归属及作废/退回互斥规则 |
| 2026-08-25 | S07 工厂小程序创建入口与一级导航复核 | 创建发货单入口按已批准原型放在“任务—订单”列表底部，不放在单个任务详情；任务、发货记录和我的统一使用三项底部导航 |
| 2026-08-27 | S10 本地实现完成并合并到 `main` | 返修分批发回、自动完成、管理员归档和三端真实进度已通过 `57f0d07` 快进合并；用户自行检查前端页面并授权合并，本轮未追加浏览器或微信开发者工具验证，未推送远端 |
| 2026-08-27 | S11 管理员通知原型范围确认 | 管理员网页通知原型已确认；管理员小程序“我的”删除头像区装饰圆环，姓名、管理员类型、脱敏联系电话、微信提醒授权和通知中心合并到一个无标题信息块，各行只用细分割线区分；授权入口显示“去授权/本次已授权”，不新增授权页面，也不提供关闭或类别开关；通知中心入口只显示数字未读圆圈和进入箭头，未读为 0 时隐藏圆圈；底部“订单、发货、我的”均不显示通知红点，合同出货、正常发货和质检单返修三类系统通知默认全部开启。微信外部提醒仍取决于平台授权 |
| 2026-08-27 | S11 工厂通知原型范围确认 | 工厂小程序“我的”按管理员小程序结构调整：头像区删除装饰圆环并展示姓名与职位；无标题信息块保留姓名、脱敏联系电话、职位和所属工厂，连续接入微信提醒授权与通知中心；不提供通知设置或类别开关；V1.56 后新订单任务、合同出货提醒、新返修任务和业务处理结果默认全部开启；通知中心使用全部/未读、首屏 10 条触底加载，通知进入对应订单任务、发货单或返修任务详情并返回原通知状态；底部“任务、发货记录、我的”均不显示通知红点 |
| 2026-08-27 | V1.51 合同出货提醒改用飞书 | 合同出货 D-10、D-5、D-3、D0 继续在管理员网页和管理员小程序生成站内通知，外部只通过飞书发送；对应跟单人员接收全部节点，两名特别关注管理员只接收 D-3；不消耗微信订阅授权。工厂的新订单、新返修和业务处理结果继续使用小程序订阅消息，站内通知始终保留 |
| 2026-08-27 | V1.52 工厂首次登录微信提醒引导 | 已启用工厂账号首次登录成功后，在任务页上方显示一次“开启微信提醒”引导，操作为“暂不开启、开启提醒”；引导不阻断使用且关闭后不重复强制弹出，“我的—微信提醒授权”继续保留；微信一次性订阅按当次授权机会处理，不承诺永久可发送 |
| 2026-08-27 | V1.53 取消工厂首次登录微信提醒引导 | 按用户最终确认撤销 V1.52 的首次登录引导，登录流程不增加订阅提示；两类小程序仅保留“我的—微信提醒授权”主动入口。微信登录不会自动授予订阅消息权限，未主动授权只影响微信外部提醒，不影响站内通知中心 |
| 2026-08-27 | V1.54 工厂登录触发微信原生订阅授权 | V1.53 继续有效地取消自建提醒引导，但调整“登录不触发”口径：已启用工厂账号点击“微信授权登录”并完成账号状态确认后调用微信原生订阅接口，由微信底部授权面板让用户选择；允许、拒绝或关闭均继续进入任务页，非启用账号不触发；“我的—微信提醒授权”继续作为再次请求入口，微信登录本身不等于已经同意订阅 |
| 2026-08-27 | V1.55 管理员登录同样触发微信原生订阅授权 | 补齐 V1.54 仅描述工厂端的遗漏：已启用管理员账号登录时请求正常发货和质检单返修模板，管理员合同出货提醒不请求微信模板并继续只走飞书；管理员和工厂允许、拒绝或关闭授权均不阻断登录，两端“我的—微信提醒授权”均保留再次请求入口 |
| 2026-08-27 | V1.56 工厂增加合同出货提醒并完成 S11 确认 | 合同出货 D-10、D-5、D-3、D0 同时通知订单所涉工厂全部已启用用户，工厂通知中心保留站内通知并通过微信订阅消息外发；跟单人员及特别关注管理员仍只通过飞书外发，特别关注管理员为煎饼和核桃；工厂四类业务通知映射到不超过 3 个微信模板 ID，优先共用字段兼容的订单提醒模板；运维告警通过飞书私聊发送给煎饼，飞书自身失败时保留数据库状态和服务器日志；S11 页面、正式决策和 TDD 公共接缝确认完成，实施门开放 |
| 2026-08-27 | S11 本地实现完成，等待原始仓库微信开发者工具复验和用户验收 | 通知授权、站内通知、外部投递与告警状态、合同出货四节点、审计查询、管理员网页和两类小程序已实现；隔离 MySQL 8 后端 139 项通过、1 项因 S06 MinIO 测试桶未配置跳过，Node.js 24 管理员网页 26 项和小程序 18 项通过，OpenAPI/类型与实际 Edge 浏览器检查通过；Docker 基础镜像元数据因网络超时未拉取成功。真实模板、飞书映射、真机、真实外部送达、远程 CI、部署和发布仍未验收，分支未合并、未推送 |
| 2026-08-27 | S11 原始仓库微信开发者工具复验完成 | 原始仓库已切到 `codex/s11-miniprogram-validation` 并升级本地开发库到 S11 head；微信开发者工具 Stable 2.01.2510290 编译为 0 errors，工厂真实会话成功读取本人资料、未读数和通知列表，工厂“我的”及通知中心全部/未读空状态正常；管理员“我的”通过 DevTools 预览接缝检查，管理员真实身份切换和真机留给用户继续验收；未配置真实模板 ID，不触发真实订阅或外部送达 |
| 2026-08-28 | S11 真实微信模板接入、推送与远程 CI 通过 | 后台已选 4 个模板并按工厂 3 个、管理员 2 个模板 ID 请求授权；受控真实微信适配器和安全开关以 `820f88c` 提交并推送 `main`；GitHub Actions CI `33144349532` 的后端、网页、小程序与 Docker 构建全部通过；未创建版本标签、未发布 GHCR、未部署或真实发送 |
| 2026-08-28 | 开始编写 S12 正式工单草案 | S12 按环境只读盘点、代码/部署补齐、共享测试发布、真实外部联调、全流程验收、备份恢复/回滚、试运行/生产准备分阶段执行；工单确认前不修改部署代码，不操作 ECS、DNS、数据库、对象存储、GHCR、小程序或真实消息 |
| 2026-08-28 | S12 工单确认并开始 A/B 阶段 | 保留完整远程 CI 门，首轮共享测试采用公司教程的 SSH + 只读 Git + Docker Compose，不建设自动 CD；测试/生产隔离，先内部健康再 Traefik/HTTPS，真实外部联调继续单独确认。只读 ECS 盘点发现根分区仅约 9.2 GiB 可用且无项目 MySQL/MinIO 备份证据，C 阶段发布前必须先确认容量处理和备份条件；未执行服务器写操作 |
| 2026-08-28 | S12 正式对象存储改为阿里云 OSS | 用户在首次共享测试部署准备中确认不使用服务器 MinIO，改为阿里云 OSS 原生 SDK；共享测试和生产各使用一个独立私有 Bucket 与独立 RAM 凭证，单桶内按 `contracts/`、`repairs/`、`avatars/`、`products/` 前缀分类。本轮只授权本地非 Docker 修改与测试，暂不提交、推送、运行远程 CI 或构建镜像，服务器容器启动暂停到新提交完整 CI 通过之后 |
| 2026-09-01 | 产品和规格身份只使用聚水潭源编码 | 真实首次扫描证明产品名称可跨 `i_id` 重复，同一 `i_id` 内颜色/规格也可对应多个 `sku_id`；因此产品以 `i_id` 唯一、规格以 `sku_id` 唯一，名称和颜色/规格仅作展示与严格核对字段 |
| 2026-09-01 | S12 正式环境目录与配置文件命名确认 | 正式环境服务器项目目录固定为 `/home/jianbing/apps/order-tracking`，正式环境受控配置文件固定为项目 `deploy/` 目录下的 `.env`。共享测试环境现有目录和 `.env.shared-test` 不受影响。当前 `compose.production.yaml`、`production.env.example` 和部署脚本仍使用 `.env.production`，必须在首次正式部署前经单独授权同步改为 `.env` 并完成验证；本决策不授权现在创建正式目录、修改服务器或启动生产部署 |
| 2026-09-03 | V1.57 取消普通管理员申请审核 | 管理员网页端完成飞书授权并取得有效已验证手机号后自动获得普通管理员权限，不再提交申请或由最高管理员审核；最高管理员仍由生产环境预置或受控配置授予，已停用普通管理员重新授权后保持停用；管理员小程序按手机号唯一匹配网页端已建立的内部管理员。页面地图、两类原型和技术设计已于 2026-09-03 同步到 V1.57，三端运行代码仍是旧流程，须由 GitHub Issue #18 后续修改并验证 |
| 2026-09-04 | V1.58 最高管理员改为同入口 OAuth | 最高管理员与普通管理员使用同一飞书 OAuth 入口；服务器按受控白名单中的飞书唯一标识授予最高管理员标记，不按姓名、手机号或登录顺序判定，不再执行最高管理员数据库初始化命令。普通管理员继续可查看和管理工厂人员，仅普通管理员账号启停仍为最高管理员专属权限 |
| 2026-09-04 | Issue #18 本地分支实现完成，未进入共享测试 | `codex/issue-18-admin-auto-grant` 已完成同入口 OAuth 自动授予、最高管理员 `tenant_key:open_id` 白名单、管理员申请 API/页面删除、小程序按已建立管理员手机号绑定及停用账号重新 OAuth 拒绝。本地后端 192 项通过、1 项 OSS 因未配测试桶跳过，网页 27 项和小程序 19 项通过，静态检查、构建、OpenAPI 漂移与本地实际浏览器回归通过。本轮不推送、不运行远程 CI、不部署；用户截图中的共享测试环境仍是旧版本 |
| 2026-09-04 | Issue #6 飞书新订单增量读取本地修复完成 | `codex/issue-6-feishu-incremental-import` 已修正累计页重复处理，增加真实来源修改时间、来源范围级成功水位、安全重叠日窗口、完整订单快照重组和已导入/已排除快照冻结；本地 MySQL 8 迁移往返、后端 199 项、静态检查和 OpenAPI 校验通过。飞书指定筛选时会忽略 `view_id`，因此真实启用前仍需单独只读确认指定视图与同表范围等价；未确认时配置门禁保持关闭。本轮未推送、未创建 PR、未部署或读取真实飞书 |
| 2026-09-04 | Issue #9 发货凭证本地修复完成，等待用户小程序验证 | `codex/issue-9-shipment-evidence` 已补齐 0 至 3 张凭证压缩上传、私有文件元数据与发货单关系、上传失败安全重试、同厂与管理员受控读取、非授权工厂 404，以及网页和两类小程序详情的加载、失败和无图状态；OpenAPI 与两端生成类型已同步。管理员网页已用本地真实 API 和私有文件适配器检查图片显示；用户明确自行完成微信开发者工具和真机验证。本轮未推送、未创建 PR、未部署或修改共享测试配置。 |

## 5. 更新规则

- 阶段完成后，更新“当前状态”和后续步骤，不在这里复制需求正文；
- 新增业务字段、状态、权限或验收口径时，先修改需求文档并由用户确认；
- 改变页面交互时更新原型，改变实现方式时更新技术设计。

## 2026-09-05 Issue #8 范围与执行门

用户确认管理员新外部业务通知迁移飞书，工厂维持现状；保留管理员微信授权交互和历史投递任务。具体卡片及接收人以需求文档 Issue #8 修订节为准，返修三个数量均已确认取本次发回批次。当前仅建立分支和同步资料，等待用户授权正式代码实施；代码与必要测试完成后停止，后续交付按用户指令。

基线 main `bb3e13fc19975602c66923da899345f93a74c7cc`，完整 CI `33940831938` 五项任务成功。分支 `codex/issue-8-admin-feishu-notifications`。本轮未修改业务代码、未提交、未推送、未部署或真实发送。

### Issue #8 本地实施记录

用户随后确认开始代码实施。管理员三类新外部通知已实现飞书卡片和四人精确名单筛选，工厂、合同提醒、授权交互及历史微信投递保持原状。后端完整测试 212 通过、1 项隔离 OSS 桶未配置跳过，小程序 29 通过；最终验证、未提交文件和后续执行命令见 `docs/一期/project/issue-8-本地交付与后续步骤.md`。本轮按用户要求停在本地，未提交/推送、未创建 PR、未部署或真实发送。

## 2026-09-05 Issue #26 执行记录

用户确认在 #8 上追加固定接收人核桃；发货与撤回追加所涉订单跟单人，质检返修因无订单关联仅通知固定五人。已建立 Issue #26，并从 main `60dbcbef26d5abadb2a1e894c81fb8d80234df62`（完整 CI 33952983090 成功）建立 `codex/issue-26-notification-recipients`。授权文档、代码与本地测试，完成后不提交。

#26 开工时，#16 尚处于工厂字母代码规则与迁移方案确认阶段；后续确认及本地实施结果见下方 Issue #16 记录，服务器数据迁移仍需另行授权。

本地实施完成：通知服务加入核桃及发货/撤回对应订单跟单人，返修仍仅固定五人。MySQL 8 通知专项 31 通过，Ruff、mypy 通过。保留未提交修改，交接与后续范围见 `docs/一期/project/issue-26-本地交付与后续步骤.md`；未进行远程 CI、部署或真实发送。


## 2026-09-05 Issue #30 本地修复

用户授权在独立分支 `codex/issue-30-order-related-shipments` 修改代码及文档，测试后本地提交即停止。当前工作树从 `74d137b` 建立分支，保留该基线，不混入 #28 返回逻辑。用户明确不核实单个订单的服务器数据，并确认保留正式页面现有六列表头。

订单详情固定空状态已替换为按订单精确查询的真实发货单列表，复用管理员发货接口，以发货明细、订单派工、订单明细关系筛选；加载失败单独提示，单号和详情链接进入现有详情页。范围与字段差异详见 S07 工单 Issue #30 补充节。

后端全量 218 通过、1 项 OSS 跳过，网页 31 通过；静态/类型、网页构建、OpenAPI 和小程序生成类型检查通过。本地浏览器受控响应验证列表、空状态、错误提示和详情跳转，未查询真实订单或进行共享测试业务验收。本轮完成本地提交后停止，不推送、不创建 PR、不部署、不写服务器数据。
## 2026-09-05 Issue #16 执行门

用户确认字母校验、括号/横杠截断、存量中文拼音首字母转换、歧义与重复组留空人工处理及历史合同保护。授权更新 Issue #16，在 `codex/issue-16-factory-code-spec` 独立工作树实现代码、一次性迁移脚本及文档，完成本地验证并提交后停止；不推送、不创建 PR、不部署、不运行服务器迁移。基线 main `60dbcbe` 完整 CI `33952983090` 成功。主工作区 #26 改动独立保留。用户随后明确本次不用对照原型，停止视觉对照，继续代码和数据规则验证。

### Issue #16 本地验证记录

工厂代码校验、NULL 空值、一次性预览/备份/事务转换/审计/回滚、管理员表单及历史合同保护已实现。后端全量 230 通过、1 项隔离 OSS 桶未配置跳过，最终迁移与合同定向 14 通过；网页全量 35 通过、最终工厂页面 7 项复验通过，类型/静态检查/构建以及 MySQL 字段迁移往返通过。按用户要求停止原型对照；范围、命令、验证和环境执行边界见 `docs/一期/project/issue-16-工厂代码转换与本地交付.md`。本地提交后停止，环境迁移与业务验收未完成。


## 2026-09-05 Issue #28 本地返回修复

用户确认文件范围并授权在当前工作区从最新 main 建立 `codex/issue-28-shipment-detail-back`，完成本地提交后停止。基线 `ef34f5d` 的远程完整 CI 已通过。两类发货详情接入共用返回兜底，正常上一页保留，无上一页或返回失败时核验当前身份后进入对应角色发货列表，身份不可用时回登录入口。

修改 3 个小程序代码文件、2 个测试文件及 S11/本记录共 7 个文件；小程序全量 40 项通过，ESLint、类型和项目结构构建检查通过。页面外观未修改；真机、开发者工具编译及原型并排检查未执行，不能声称 Issue #28 完成真实验收。详细范围、自动化证据和后续冷/热启动验收见 S11 工单末尾。下一步仅在另获授权后推送/PR、远程 CI 与真机验收，不部署、不发布、不发送真实通知。

## 2026-09-05 Issue #12 本地修复

用户确认在当前工作区从 `main` 提交 `51eabb9`（完整 CI `33961579032` 成功）建立 `codex/issue-12-mini-date-notification-layout`，时间统一显示为 `YYYY-MM-DD HH:mm`，完成本地提交后停止。

两类角色共用的通知中心和工厂申请状态页已接入同一上海时区日期工具。服务端无偏移 ISO 时间按现有无时区 UTC 存储口径解释，显式 UTC、显式偏移和微秒时间均转换为上海时间；非法或缺失时间显示 `—`。通知中心类别和时间保持单行，长标题单行省略，摘要最多两行，原有全部/未读、分页与详情跳转不变。

Node.js 24 下小程序全量 44 项测试、ESLint、类型检查和项目结构构建通过；微信开发者工具 Stable 2.01.2510290 对通知中心窄屏样例和工厂申请审核中页编译为 0 errors，并完成页面检查。通知样例仅用于本地视觉检查，没有写入运行代码。未连接本地真实 API，iOS/Android 真机、共享测试、远程 CI、部署和发布均未执行。
## 2026-09-05 Issue #15 本地通知与看板修复

用户确认修改清单并授权至本地提交后停止。基于 main `51eabb9`（完整 CI `33961579032` 成功），在独立 Worktree 分支 `codex/issue-15-admin-notifications` 完成网页通知未读摘要、读态同步、网格排版及今日发货/逾期订单筛选跳转。网页 45 项测试、静态检查、类型与构建通过，Edge 本地测试数据视觉检查完成；详情与既有原型差异及统计接口遗留见 S11 工单末尾。未修改后端、小程序或原型；未推送、未创建 PR、未部署，真实业务验收未完成。


## 2026-09-06 Issue #14 范围收窄与文档同步

用户确认产品资料页保留图片并增加点击放大预览；订单详情、待导入订单详情、发货单详情删除产品图片列。加工合同原有图片能力和订单图片快照保留，发货凭证照片保持原状；已核对当前发货清单生成代码及运行模板不包含产品图片，本次不修改发货清单导出。

本轮授权仅更新 GitHub Issue #14、需求文档和 S03/S04/S05/S07 工单及本记录；明确不修改原型、业务代码或测试代码。原型未同步，已在需求和工单记录图片列差异；预览具体关闭交互待实施前说明。未进行功能测试、远程 CI、部署或业务验收。

本地交接：本轮仅上述 6 份文档存在未提交修改；完成文档差异与空白检查后保留本地，不提交或推送。后续范围为另获实施授权后按修订后的 #14 开发和验证，与 #29 的小程序弹窗改动隔离。

### Issue #14 本地实施与停止点

用户随后授权代码修改和测试，完成后停止。已在独立分支 `codex/issue-14-product-image-preview` 实现产品资料图片预览、三个详情表删图片列及必要列宽适配；原型仍不修改。网页 47 项测试、静态/类型检查和构建通过，Edge 本地受控响应及四页原型并排检查完成。实际图片权限/缓存联调和共享测试验收未完成；未提交、推送、创建 PR 或部署。修改文件、验证边界及后续步骤见 `docs/一期/project/issue-14-本地交付与后续步骤.md`。

## 2026-09-06 Issue #11 页面确认与文档执行门

用户确认在当前工作区执行 #11，采用当前箱明细卡片内部底部的浅蓝次级“应用到连续箱”按钮，点击后填写后续连续箱数并展示目标范围；统一产品、规格和订单下拉 SVG，保留四步流程。逐箱及批量录入均允许多发，继续要求明确订单派工归属、数量展示及提交前二次确认，不采用 Issue 原“超过剩余可发数量直接阻止”口径。

本轮仅授权修改相关文档并停止。当前工作区建立 `codex/issue-11-batch-packing`，基线 `761b4f0`，未更新运行代码；代码开工前重新核对最新 main 与完整 CI。需求第 4.2 节和 S07 工单已同步确认规则，视觉参考、预计修改范围及未提交交接见 [S07 Issue #11](work-orders/S07-正常装箱发货闭环.md)。GitHub Issue 原验收项尚未同步；代码实施须等待用户再次明确授权。本轮未修改业务代码或原型代码，未提交、推送、部署、发布或发送真实消息。

### Issue #11 本地代码与测试完成，等待用户编译查看

用户随后授权修改代码并测试后停止，明确由自己在微信开发者工具编译查看。当前分支 `codex/issue-11-batch-packing` 已更新至完整 CI 通过的 main `d3b6712`，此前 #11 文档和参考图完整保留。创建发货单已接入明细卡片底部的连续箱复制按钮与数量弹窗、单箱解除组关系、自动保存/恢复、统一下拉 SVG，以及允许多发的数量展示和提交前确认；复用既有后端接口，无数据库或原型代码改动。

Node.js 24 下小程序 56 项测试、ESLint、类型检查、项目结构构建及空白检查通过。微信开发者工具编译、实际页面视觉、真机及真实 API 验证未执行，交由用户继续；完整未提交清单与证据见 S07 工单末尾。当前停止，不提交、推送、部署、发布或真实发送，GitHub #11 验收文本仍待后续同步。

### Issue #11 本地提交授权

用户确认多发沿用所选订单、批量不自动跨订单分配的逻辑，并授权本地提交；推送和创建 PR 只提供指令，由用户执行。设备与共享测试验收证据边界保持不变，PR 正文现见已合并的 [PR #40](https://github.com/kocotree/order_tracking/pull/40)。
## 2026-09-06 Issue #38 人员管理本地调整

用户确认最高管理员专属“管理员”页签，以及所有管理员可见的“工厂用户申请、工厂用户列表”。复用现有管理员页面及权限，工厂名单不再混入管理员；不恢复管理员审批，分页交给 #17。用户授权本地修改、测试和提交后停止，并明确跳过浏览器检查。

网页全量 49 项测试、ESLint、类型检查及构建通过；未改后端、数据库或小程序。浏览器、共享测试和本分支远程 CI 未执行，未推送或创建 PR。具体修改和证据见 S02 工单 Issue #38 记录。

## 2026-09-06 Issue #17 全站分页本地实施

#38 / PR #39 已合并且 main `648479a` 完整 CI 通过；当前 Worktree 在 `codex/issue-17-numeric-pagination` 实施九个管理员列表统一分页。用户确认产品资料页外观、每页 10 条、最多 7 个位置（含省略号）；看板仅 10 条无页码。人员服务端分页、返修超过 100 条完整访问、筛选与越界回退已完成，接口生成类型同步。

网页全量 60 项通过，后端隔离 MySQL 8 全量 239 通过、1 项 OSS 桶未配置跳过；静态/类型、网页构建、OpenAPI 与小程序类型检查通过。Edge 受控数据交互、双视口序号计算样式和代表页面截图检查完成。真实端到端 API 浏览器、共享测试和本分支远程 CI 未执行。本地提交后停止，不推送、不创建 PR、不部署。修改范围、命令对应的验证结果及全量加载限制见 S02 工单 Issue #17 交付节。

## 2026-09-07 Issue #44 规则确认与资料同步

用户确认管理员网页按箱核对，不填原因；保存只暂存，整单确认后同步订单和工厂数量并锁定；允许非负整数，原报后台保留，工厂详情在发货明细上方按订单/SKU显示多收或少收，明细直接显示确认数量。其余边界按需求V1.59第4.3节，包括撤回/退回互斥、净少收恢复已完成订单、清单保留原报、无旧正式单兼容、复用工厂单据状态通知且无差异也通知。未授权清理测试数据。

用户确认网页采用Playwright自动化，不另做Browser手动操作；旧原型尚未同步，本次不读、不改、不对照，以已确认截图和规则为依据。小程序开发者工具、真机和真实通知送达由用户负责，自动化通过不替代该验收。

当前工作区从干净的detached HEAD `c5aaad9`建立`codex/issue-44-receipt-spec`，本轮先同步Issue和正式资料，不修改运行代码、迁移或测试代码。技术方案保留原报正整数结构，独立保存可为0的收货结果，确认时以事务追加差额。资料与功能完成分开记录；实现前复核最新main、完整CI及本轮技术方案。

本地未提交文件及后续范围见`docs/一期/project/issue-44-规则确认与交接.md`。本轮不提交、推送、部署、执行迁移、发布小程序或真实发送。


### Issue #44 代码与本地功能测试（2026-09-07）

用户已明确授权代码修改与功能测试，指定 Edge 执行 Playwright；该授权承接上一节资料同步阶段。当前分支沿用 `codex/issue-44-receipt-spec`，基线 `c5aaad9` 与最新 main 一致、完整 CI `34010875737` 成功。

已实现独立收货核对结构、网页草稿和整单确认、三端生效数量、退回/作废确认基数、原报导出及工厂确认结果通知。详细文件、验证证据和后续范围见 [Issue #44 交接](issue-44-规则确认与交接.md)。保留未提交修改，不提交、推送、部署、发布或真实发送；旧原型检查按用户明确指令豁免，小程序开发者工具/真机与真实通知由用户验收。


## 2026-09-07 Issue #52 返修发回服务器草稿

用户授权另建 Issue 和分支，在当前工作区实施返修发回草稿并创建关联开发任务，备注留空。已建立 codex/issue-52-repair-return-draft，基线 origin/main f74c84d；上一轮 #43 后续视觉提交保留在独立分支，未混入本分支。

定义和验收补充至需求及 S10：按用户/返修单隔离，保存未完成输入，恢复最新数量口径，版本保护和提交原子清理，不影响正式数量。GitHub #52 与开发任务已创建并关联。新增服务器迁移只在本机隔离测试库运行；本轮本地测试与提交后停止，不推送、不创建 PR，不执行浏览器、微信或线上验收、部署及小程序上传。
## 2026-09-07 Web 原型按当前前端同步，待用户视觉核对

用户明确授权先整理 Web 差异并按代码修改原型，自行打开验证，不查看小程序、不运行浏览器。干净工作区从 `codex/issue-47-notification-ui` / `50747abd` 建立 `codex/web-prototype-sync`。更新范围、前端来源、样式入口和本地验证见 [Web 原型同步差异清单](Web原型同步差异清单.md)。

原型主要业务页面已按当前代码更新，21 个 JS 语法及 50 个现有示例路由 DOM 模拟检查通过；不是视觉验收。手工新建/编辑草稿页面仍有订单日期展示与正式需求冲突，未新增该原型页面。保留原型与三份相关文档的未提交修改；正式前端、后端和小程序未修改，未提交、推送或部署。


## 2026-09-08 Issue #58 标签自动部署确认

用户确认生产发布改为：main 同提交完整 CI 通过 → 主动推送版本标签 → 自动核验 CI 结果 → 发布两个镜像 → 自动备份、迁移、部署和健康检查。发布和 CD 不再重复运行完整 CI；推标签即该版本生产部署授权，普通分支/main 推送不部署。
服务器版本按最新确认从 v1.0.0 开始，独立于小程序版本；v1.0.0 部署验证成功后清理误用的 v1.0.6 标签及专属镜像版本，保留历史备份与发布记录。实现及验收见 Issue #58 与 `issue-58-生产CD接入与首次部署.md`，覆盖 S12 早期关于生产手动触发的旧安排。


## 2026-09-08 Issue #61 明细合同出货时间规则与文档停止点

用户确认修改文档并新建一个GitHub Issue，检查后再开始改业务代码。当前干净main建立`codex/issue-61-contract-dates`，新建[Issue #61：合同出货时间改为工厂SKU明细日期并修正飞书来源](https://github.com/kocotree/order_tracking/issues/61)，作为唯一执行工单，不新增同内容Markdown工单。

已确认：来源为飞书原列“合同出货时间”，按订单＋SKU＋工厂保存；来源减4个自然日，人工日期不再减；待导入／草稿可改，已发布不可改；来源为空或单条多值留空，不增日期异常横幅，但空日期不能导入／发布；顶部去重升序汇总；任一未交完明细逾期即未完成主订单逾期；合同日期列与正文交货期限不填具体日期；待导入明细日期列占用产品名称宽度并调整规格／工厂列。具体保存、重复拉取、筛选排序、提醒去重及页面尺寸等未批准细节见需求第3.4.1节及Issue。

用户已选择后续清理正式环境现有订单，测试环境不清；本轮没有清理。关联数据、独立返修、合同文件、通知队列和审计的删除／保留清单待盘点审查，人员／工厂／产品／配置保留，备份恢复和执行窗口明确后再执行，最后由用户重新拉取。不得把本轮文档授权解释为部署或清理授权。

### 本地未提交文件范围

- `AGENTS.md`：仅同步需求基线V1.60，不改变协作与授权规则。
- `CONTEXT.md`、`docs/README.md`、`docs/一期/requirements/一期需求文档.md`、本记录：术语、规则、入口和停止点。
- `docs/一期/project/work-orders/` 下S04、S05、S06、S07、S08、S11：仅顶部规则替代说明和Issue链接；历史过程不重写。
- `docs/一期/project/一期技术设计方案.md`、`docs/一期/project/一期开发计划.md`：仅顶部“新日期设计／计划待确认”说明。
- `docs/shared/管理员网页端表格视觉规范.md`：记录新增日期列及列宽调整方向，精确尺寸和原型仍待确认。

### 验证与后续范围

本轮只做文档差异、空白、相对链接及新Issue回读检查，不运行业务测试、构建、浏览器或微信工具；这些检查不代表业务验收。业务代码、测试、迁移、原型、飞书Base数据及正式／测试环境均未修改；未提交、推送、创建PR、部署、清理或发送真实通知。

下一步是用户检查Issue与本地文档，再按原型、技术设计、开发计划和代码确认门继续；按需求第3.4.1节最新补充确认及剩余方案边界执行。GitHub正文链接指向main，本轮文档尚未提交，所以远程文件可能仍显示旧版本；本地文档为本轮审查对象。


### Issue #61 五项业务选择收口（2026-09-08）

用户对五项问题回复“全都确认”，按各题推荐项执行：待导入日期自动保存，失败明确提示，再拉取保留人工修改；同订单＋SKU＋工厂多来源日期冲突留空，人工填写一个日期后导入；日期筛选任一可见明细命中即可，升序取最早、降序取最晚、缺失最后；工厂只按本厂明细判断逾期；同订单同日期未交完明细按接收人合并提醒，工厂仅含本厂内容，不同日期分开，节点不变。

这五项覆盖上一节当时的业务待确认状态。已同步需求第3.4.1节、验收项、CONTEXT及Issue正文；原型具体展示、技术方案和正式清理清单仍按后续阶段处理。当前仍仅文档／Issue授权，没有开始业务代码、测试、迁移、原型或数据清理，未提交或推送。


## 2026-09-08 Issue #61 本地代码实施

用户授权代码修改、测试、本地提交后停止，明确不运行Playwright，并要求前后端启动后的前端链接。随后指定不用独立Worktree，先提交文档；文档提交为`661be33`。临时Worktree仅复制过本轮文档，确认内容完全相同后已移除，业务代码均在当前`codex/issue-61-contract-dates`实施。

已接入来源列修正、四自然日转换、候选人工覆盖与版本、派工日期、三端汇总和明细、筛选排序、本厂逾期、按日期提醒及合同留白。实现细节见技术设计Issue #61专节；规格／工厂／日期列宽和交互见共享表格规范末尾。原型未同步，用户通过本地正式页面自行查看；不把源码尺寸或构建通过当作视觉验收。

本地前端`http://127.0.0.1:5175/`，API`http://127.0.0.1:8000`，使用本机开发库及local_demo身份；网页点击飞书登录可选择演示管理员。没有启动真实外发worker。开发库仅执行非破坏性字段迁移，没有清空订单；测试使用独立MySQL 8测试库，并另建临时隔离库完成升级前数据回填／降级／再次升级验证，临时库已清除。

最终本地验证：后端完整测试288通过、1跳过（未配置隔离OSS测试桶）；管理员网页端68通过，小程序83通过；后端Ruff、mypy（68个源文件）及OpenAPI一致性检查通过，网页端和小程序lint、类型检查与构建通过。独立MySQL 8临时库验证旧日期及数量回填、降级和再次升级均通过；开发库升级完成。后端重启至本轮代码后`/health/ready`返回ready，前端返回HTTP 200。仓库空白检查通过。

未执行Playwright、浏览器视觉验收、微信开发者工具、远程CI、真实OSS和真实通知验证。停止点是本地提交，不推送、创建PR、部署或清理正式数据。Issue #61仍待远程CI、发布、小程序／业务验收及正式重取数据阶段，不能提前关闭。

## 2026-09-08 Issue #63 待导入订单加载优化

用户确认仅优化页面进入请求顺序与候选列表数据库查询；在当前干净工作区从`main 43ce1a9`创建`codex/issue-63-order-import-loading`，执行到本地提交停止。[Issue #63](https://github.com/kocotree/order_tracking/issues/63)为本轮工单；基线main完整CI `34190806745`已通过。沿用一期V1.60、S05、#17分页及#61日期口径，不新增业务或视觉规则。

修改范围：

- `admin-web/src/pages/OrderImportPage.vue`：首次候选、工厂、最近任务请求独立启动；候选返回立即展示。辅助失败独立处理，保留列表请求序号和导入轮询，防止最近任务迟到与手动获取重复轮询、离页后补启动轮询。
- `server/app/modules/order_import/service.py`：关联`EXISTS`筛选避免一对多重复计数；数据库完成全局筛选、计数、排序、分页，只批量读取当前页明细。所有白名单排序及同值更新时间降序保持；产品/工厂按明细ID顺序拼接，查询级MySQL提示提高拼接及排序长度，避免默认短截断，且不修改连接池会话或全局配置。二进制NO PAD排序与SQL内Unicode折叠保留原Python字符串口径，搜索中的百分号、下划线和反斜杠按字面匹配。
- `admin-web/src/__tests__/OrderImportPage.spec.ts`和`server/tests/integration/test_order_import.py`：增加慢/失败辅助请求、乱序、轮询竞态，以及所有排序双向/跨页、组合筛选、空明细、排除状态、完整响应和日期汇总、Unicode与长明细拼接、查询次数回归。
- 本记录：范围、验证和停止点。

性能证据来自本机独立MySQL 8测试库，未访问正式接口或正式数据库。每个候选1条明细，读取第2页、每页10条；统计包含管理员校验在内的SQL语句次数。旧版通过临时测试钩子加载`43ce1a9`的原始`list_candidates`方法，与新版使用同一套测试数据。旧版6组均触发新增的最多4次SQL断言，构成优化前证据。

| 排序 | 候选数 | 优化前SQL次数 | 优化后SQL次数 |
|---|---:|---:|---:|
| 默认 | 20 | 32 | 4 |
| 默认 | 100 | 112 | 4 |
| 产品名称 | 20 | 52 | 4 |
| 产品名称 | 100 | 212 | 4 |
| 工厂 | 20 | 52 | 4 |
| 工厂 | 100 | 212 | 4 |

新版非空页固定为管理员校验、计数、候选分页、当前页明细4次SQL；空页省略明细查询。查询次数不再随候选数量逐单增长，不表示数据库扫描或排序成本为常数，也不把本地SQL次数换算为正式接口提速百分比。

最终验证：后端隔离MySQL 8全量298通过、1跳过（未配置隔离OSS测试桶）；随后针对最终候选查询代码复跑导入模块27项全部通过，6组查询次数均为4。管理员网页端全量74项通过；后端Ruff、mypy（68个源文件）、OpenAPI一致性检查，以及网页端ESLint、类型检查和构建通过。仓库空白检查通过。

页面组件测试先在旧代码复现工厂未返回阻塞任务/列表请求，随后验证并行展示、辅助失败、旧列表请求迟到、活动任务成功只刷新一次、手动获取和最近任务竞态、离页后历史/在途轮询返回不继续请求。服务集成测试以未改动详情接口的快照和旧Python排序筛选规则作为对照，逐项核对结果、总数、明细、日期汇总；另验证字面通配符、Unicode折叠、超过4KB的完整拼接排序与多明细重复命中。

本地前端`http://127.0.0.1:5175/orders/import`，API`http://127.0.0.1:8000`，使用本机开发库和`local_demo`身份；可点击飞书登录选择演示管理员。测试库与开发库隔离，未执行正式数据、飞书写入或业务数据库结构修改，没有启动真实外发worker。

未验证：正式接口耗时、真实数据规模的执行计划/延迟、浏览器视觉与用户体验、远程CI、真实飞书及通知。按用户要求未运行Playwright或其他浏览器自动化；未推送、创建PR或部署。Issue保持打开，后续发布与真实验收需要另行授权。

## 2026-09-09：Issue #65 工厂自主撤回与原单号重新发货（本地）

用户最终确认后在 `codex/issue-65-shipment-withdraw` 实施。撤回直接冲销有效数量、恢复受影响已完成订单，保留原单号与正式历史；独立编辑草稿仅该轮提交人和撤回人可用，个人普通草稿保持独立。恢复、保存、附件修改及重新提交处理版本冲突与幂等，管理员核对草稿失效并保留审计。通知固定各轮事实，撤回结果替代新申请审核。

修改范围：服务器模型、迁移 `20260909_0032`、发货事务与权限、通知、OpenAPI；管理员网页发货列表/详情；工厂小程序发货列表/详情/装箱编辑、管理员小程序发货列表/详情及接口定义；对应需求、设计、领域术语和使用说明。旧审核 HTTP 写入口关闭，历史数据结构保留。设计入口见 [Issue #65 页面与技术设计](issue-65-页面与技术设计.md)。

验证：后端隔离 MySQL 8 全量 303 项通过、1 项跳过（未配置独立 OSS 测试桶）；之后发货 API/收货回归 47 项通过，最终新增边界与通知调整后专项 10 项通过。管理员网页 76 项通过，ESLint、类型检查和构建通过。后端 Ruff、mypy（68 个源文件）和 OpenAPI 一致性检查通过；本地测试库迁移检查、0032 → 0031 → 0032 升降级及再次一致性检查通过。新专项覆盖双人编辑冲突、收货与撤回竞态、事务回滚、重复请求、跨工厂访问、附件历史与私密性、多轮数量、完成订单恢复和延迟通知事实。

按用户要求不运行 Playwright 或小程序测试、类型检查、构建、开发者工具及真机验证；仅更新小程序代码与生成的接口类型。现有旧审核 API 测试按已确认的新规则替换，不把旧流程通过作为验收标准。未进行远程 CI、共享测试/生产迁移、部署、真实通知或小程序上传。本次在本地提交后停止，Issue 保持打开；发布与未执行的端侧验收留待后续授权。



## 2026-09-09 五页面性能与首页统计范围确认

用户确认仅处理首页、订单列表、发货记录、返修退回、工厂资料及其工厂筛选选项，其他页面暂不处理；确认完整筛选选项和首页全局逾期／今日有效发货统计修正。已建立[Spec Issue #66](https://github.com/kocotree/order_tracking/issues/66)，作为后续独立执行切片的范围依据，需求文末已同步。

当前Worktree基线0fea673，分支codex/admin-page-performance-scope。本轮只新增范围和验收说明；尚未修改业务代码、技术设计或开发计划，未运行业务测试、构建、浏览器、远程CI或部署。下一步读取对应设计及原型，形成详细技术方案和独立执行Issue；不扩展到另外两条业务反馈。

本轮修改文件：一期需求文档、本里程碑记录。文档未提交；接手时须保留这两处增量。GitHub Issue已创建，飞书任务尚未回填该Issue链接。


## 2026-09-09 Issue #66 四任务并行本地执行

用户在讨论并拆分 #68–#71 后明确授权检查依赖、派发四个独立工作区，各自本地修改、测试、提交后停止，不运行 Playwright。已同步最新 main e0b81a4（完整 CI 成功），保留原范围文档和 #65 自主撤回规则；先将批准方案及执行安排写入技术设计和开发计划，再实施。

本工作区执行 #71，工厂数据库分页和订单轻量筛选选项完成；后端全量 309 通过、1 OSS 跳过，网页 78 通过，最终工厂页专项 9 通过，静态/类型/构建、OpenAPI、模型漂移检查通过。实际文件、量测和集成边界见[本地实施与验证](issue-71-本地实施与验证.md)。#68、#69、#70 在各自独立工作区提交，状态及证据以各分支专属实施记录为准，未合并到此分支。

本轮停止点是各分支本地提交；没有推送、创建 PR、合并 main、运行新远程 CI、部署或关闭 Issue。浏览器视觉与四分支整体集成仍未验证。

## Issue #76 最新范围与文档交接（2026-09-09）

本轮用户明确要求更新Issue和相关文档，仍不改代码。此前仅工厂汇总、管理员逐单的提案已被三端统一周期汇总代替；归档改为全周期返完后整体隐藏。原始质检单及发回分配保留数据库；Web去掉工厂发回历史和原箱号明细，新增SKU汇总产品明细，两个小程序保留周期发回历史。

Web上传点击/拖入共用区域、多文件逐份解析、不展示明细，以原文件名提示错误；人工确认仅创建通过项，失败可重传；同内容已创建文件拦截，同名不同内容不直接判重。正式附件按创建成功的系统退回日期命名，同厂同日多份加序号。用户说明一文件一工厂、没有旧草稿及相关归档记录、不向归档周期再上传；不开发多厂拆分、旧草稿转换或自动重开归档周期。

已同步CONTEXT、文档入口、一期需求、技术设计边界及开发计划，并更新唯一Issue #76标题和正文。具体表结构、接口及算法仍待技术设计阶段落实，不称为已实现。#70存在共享返修查询/API范围，后续集成核对其最新状态。

当前工作区已有#65等代码、迁移、测试和文档改动，本轮保留。只检查本轮文档差异、链接和空白，以及Issue回读；未运行应用测试或视觉验收，未提交、推送、迁移、部署或修改飞书。下一步审查文档和技术方案，取得代码授权后实施。


## Issue #76 本地实现与验证（2026-09-09）

后续用户授权修改业务代码、必要测试、准备本地一厂两份质检Excel，并本地提交后停止；明确不使用Playwright和微信开发者工具，由用户自行验收。使用独立Worktree分支`codex/issue-76-repair-periods`，基于已通过完整main CI的`4fe5df0`，保留原工作区#65等修改，未推送、建PR或部署。

实现三端周期列表/详情、跨原单FIFO发回及新周期草稿、一次物理批次一次历史和通知、整周期归档、日期附件命名、Web同区点击/拖入多文件及按文件报错确认。新增0033周期/跨单批次/草稿表及原单关联；迁移只在本地隔离数据库执行，不转换用户说明不存在的旧草稿和归档。

后端全量347通过、1项OSS配置跳过、1项旧schema断言失败；断言更新为含新增周期字段后，最终返修专项19项全通过，无遗留失败。Web88项、小程序83项、两端lint/类型/构建、Ruff/mypy/OpenAPI及迁移模型检查通过。迁移升降级与原始事实保留由专项测试验证。未执行最终版本全量重跑、远程CI、浏览器视觉或微信工具验收。

独立本地演示库已建一厂、两份Excel、两个SKU共70件，前后端已启动，真实HTTP读接口和附件可用。配置、身份切换、启动命令、测试与限制见[本地验证与交接](issue-76-本地验证与交接.md)。下一步仅用户人工验收及另行授权的反馈修正；发布、生产迁移和真实通知仍需独立授权。


## 2026-09-10 Issue #81 实施授权

用户确认四页面工厂名称包含搜索、多选保持、原查询触发及分页重置规则，并明确“可以开始”。本轮在 main 50b747e 基础的独立分支 codex/issue-81-factory-filter 实施，覆盖 Issue 中原先“仅建台账”的历史停止点。资料、代码、必要测试和本地提交已授权；不推送、建 PR 或部署。不使用 Playwright、浏览器开发者工具或微信开发者工具；人工视觉验收留待用户。

本地实现及验证已完成：后端相关 33 项、网页全量 97 项、Ruff/mypy/OpenAPI、网页 lint/类型/构建及小程序类型检查通过；无遗留失败。具体文件范围、人工验收步骤和未验证边界见 [Issue #81 本地实施与验证](issue-81-本地实施与验证.md)。
