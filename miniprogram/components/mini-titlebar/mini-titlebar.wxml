<view class="titlebar-shell">
  <view class="titlebar-grid">
    <button wx:if="{{showBack}}" class="titlebar-back" aria-label="返回" bindtap="goBack">‹</button>
    <view wx:else class="titlebar-spacer" />
    <text class="titlebar-title">{{title}}</text>
  </view>
</view>
