# 工厂小程序低保真原型

本目录用于逐页确认工厂小程序的页面内容、字段、操作和跳转，不是微信小程序正式实现代码。业务范围以 `docs/requirements/一期需求文档.xml` V1.33 为准，页面层级以 `docs/project/一期三端页面地图.md` 为准。

## 当前进度

| 页面或流程 | 当前状态 | 说明 |
|---|---|---|
| 正常任务列表 | 视觉已调整，待确认 | 按订单维度紧凑展示，默认显示未完成任务，包含搜索、状态和合同出货时间筛选 |
| 本厂任务详情 | 视觉已调整，待确认 | 展示本厂订单汇总和产品规格明细表格 |
| 创建发货单 | 流程已调整，待确认 | 按箱号、装箱、选填凭证、预览提交四步形成可操作闭环 |
| 质检单返修任务 | 未制作 | 包含任务列表、详情和创建返修发回记录 |
| 发货记录 | 未制作 | 包含正常发货记录和质检单返修发回记录 |
| 我的 | 未制作 | 包含账号、职位、所属工厂和业务通知设置 |
| 进入与权限状态 | 未制作 | 包含身份识别、申请工厂、审核结果和无权限状态 |

底部一级导航已经确定为“任务、发货记录、我的”。

## 已确认的原型口径

- 正常任务列表按订单维度展示，一张订单一张卡片，不按产品规格拆卡。
- 任务列表卡片不展示颜色/规格，任务详情再展示产品规格明细。
- 创建发货单从任务列表页进入，不从任务详情进入。
- 创建发货单采用“先添加箱号，再逐箱选择订单规格并填写数量，最后填写凭证并预览提交”的方向。
- 一张发货单可以包含本厂多个订单和多个产品规格，一只箱子也可以混装。
- 同一产品规格跨订单时，默认按合同出货时间最早的订单优先分配，提交前展示分配结果并允许调整。

任务列表默认显示未完成任务，已完成任务可以通过状态筛选查看。

## 创建发货单当前口径

- 工厂只填写本次发货总箱数，系统自动生成从 1 开始连续排列的箱号。
- 装箱时按“箱号—产品名称—颜色/规格—数量”逐箱录入，不要求工厂选择订单。
- 每个箱号分别填写装箱明细，不提供装箱组或相同装法批量录入。
- 发货凭证照片和工厂备注均为选填，照片最多 3 张。
- 预览页先展示产品规格汇总和装箱明细；同一规格涉及多个订单时，再按合同出货时间从早到晚分配并允许调整。
- 提交前校验装箱数量、可发数量和跨订单分配合计；提交后立即计入订单已发数量。

## 下一步讨论顺序

1. 确认创建发货单四步流程的页面效果和实际操作顺序。
2. 创建发货单确认完成后，继续质检单返修任务。

## 本地查看

```bash
cd prototype/factory-miniprogram-lowfi
python3 -m http.server 4175
```

浏览器打开 `http://127.0.0.1:4175/`。

## 原型边界

- 任务卡片可以进入任务详情；点击尚未制作的底部页签时只提示后续逐页确认。
- 当前创建发货单流程用于确认交互，不代表正式业务已经实现或验收。
- 正式开发将在 `miniprogram/` 中建立微信小程序项目，本原型不会直接作为生产代码上线。
